---
domain: integrations-and-sdk-usage
snapshot_id: null
applies_to:
  paths: ["**"]
last_verified: {{today}}
status: active
---

# Integrations & SDK Usage — Rules

_Populated by `harness scan` when it detects external SDK usage patterns._
_Activated conditionally: scanner only runs this domain if it finds SDK clients,_
_adapter packages, or HTTP client wrappers in the repo._
