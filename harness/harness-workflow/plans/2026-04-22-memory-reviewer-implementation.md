# Harness Memory + Reviewer Upgrade — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement the `strict-reviewer` skill + `harness-workflow` memory/reviewer upgrade per approved spec at `docs/superpowers/specs/2026-04-22-memory-reviewer-upgrade.md`.

**Architecture:** Two products. (1) New `strict-reviewer` skill — schema-driven thin wrapper, stateless, default-FAIL + three gates + adversarial search. (2) `harness-workflow` upgrade — adds `.harness-memory.yml` contract, `docs/memory/` scaffold templates, Stage 2–8 hooks for memory lifecycle + reviewer invocation, `team-commander` Session Health Check split.

**Tech Stack:** Markdown skill files, YAML config, bash verification helpers, git for versioning. No runtime code — everything is markdown/YAML consumed by Claude at skill-invocation time.

**Dependency order:** Phase A (foundation) → Phase B (new skill) → Phase C (harness integration) → Phase D (team-commander) → Phase E (verify + push).

---

## Preamble: Baseline check (run first)

- [ ] **Step 1: Confirm working tree clean + spec exists**

Run:
```bash
cd /Users/twelve/Music/myskills
git status --short
ls docs/superpowers/specs/2026-04-22-memory-reviewer-upgrade.md
git log --oneline -1
```

Expected:
- `git status --short` → empty (clean)
- `ls` prints the spec path
- Last commit is `d5d279f` (iteration log) or later

If working tree is dirty, commit or stash before proceeding.

- [ ] **Step 2: Confirm symlink setup intact**

Run:
```bash
readlink ~/.claude/skills/harness-workflow
readlink ~/.claude/skills/team-commander
```

Expected: both print `/Users/twelve/Music/myskills/<skill>`. If missing, run the symlink loop from README step 2 before proceeding (without it, Claude can't see changes).

---

# Phase A — Foundation (templates + memory reference rewrite)

## Task 1: Scaffold `templates/project-memory/` directory

**Files:**
- Create: `harness-workflow/templates/project-memory/cases/.gitkeep`
- Create: `harness-workflow/templates/project-memory/decisions/.gitkeep`
- Create: `harness-workflow/templates/project-memory/constraints/.gitkeep`
- Create: `harness-workflow/templates/project-memory/archive/.gitkeep`

- [ ] **Step 1: Create all four subdirs with .gitkeep**

Run:
```bash
cd /Users/twelve/Music/myskills
for sub in cases decisions constraints archive; do
  mkdir -p "harness-workflow/templates/project-memory/$sub"
  touch "harness-workflow/templates/project-memory/$sub/.gitkeep"
done
```

- [ ] **Step 2: Verify directory tree**

Run:
```bash
find harness-workflow/templates/project-memory -type f -o -type d | sort
```

Expected output:
```
harness-workflow/templates/project-memory
harness-workflow/templates/project-memory/archive
harness-workflow/templates/project-memory/archive/.gitkeep
harness-workflow/templates/project-memory/cases
harness-workflow/templates/project-memory/cases/.gitkeep
harness-workflow/templates/project-memory/constraints
harness-workflow/templates/project-memory/constraints/.gitkeep
harness-workflow/templates/project-memory/decisions
harness-workflow/templates/project-memory/decisions/.gitkeep
```

- [ ] **Step 3: Commit**

Run:
```bash
cd /Users/twelve/Music/myskills
git add harness-workflow/templates/project-memory/
git commit -m "feat(harness): scaffold project-memory template directories"
```

---

## Task 2: Write `.harness-memory.yml.template`

**Files:**
- Create: `harness-workflow/templates/project-memory/.harness-memory.yml.template`

- [ ] **Step 1: Write the template**

Full content (substitutes `{{var}}` placeholders at render time):

```yaml
# Auto-generated by harness-workflow --init / --adopt.
# Edit this file to configure harness memory behavior.
# Spec: docs/superpowers/specs/2026-04-22-memory-reviewer-upgrade.md

schema_version: "1.0.0"

project:
  name: "{{project_name}}"
  type: "{{project_type}}"
  root_fingerprint: "{{root_fingerprint}}"

# White list — harness may create/update these paths only.
owned_paths:
  - "docs/memory/.harness-memory.yml"
  - "docs/memory/harness_reviewer_scorecard.yml"
  - "docs/memory/MEMORY.md"
  - "docs/memory/ERRORS.md"
  - "docs/memory/harness_*.md"
  - "docs/memory/cases/harness_*.md"
  - "docs/memory/decisions/harness_*.md"
  - "docs/memory/constraints/harness_*.md"
  - "docs/memory/archive/harness_*.md"
  - "docs/memory/archive/harness_*.yml"

# Black list — absolute deny, wins over owned_paths.
# Must be non-empty. Add paths here that harness must NEVER touch.
forbidden_paths:
  - "docs/memory/private/**"
  - "docs/memory/team-written/**"

# When to mark a memory as `suspect` (edit to taste).
# Reference: spec §Suspect Rule 文法
suspect_rules: []

# Error case collection criteria. A bug qualifies for cases/ when
# it meets at least `min_criteria` of these.
errors_collection:
  min_criteria: 2
  criteria:
    - "diagnosis_over_30m"
    - "cross_module"
    - "repeated"
    - "platform_specific"
    - "user_visible"
    - "invalidated_assumption"

archive_policy:
  hot_index_max_lines: 200
  archive_after_days_unused: 180
  archive_if_status: ["superseded", "archived"]
  cold_dir: "docs/memory/archive"

audits:
  last_full_audit: null
  last_error_audit: null
  last_reviewer_score_audit: null
  conflicts: []

supersession:
  format: "relative-path#case-id"

reviewer:
  scorecard_path: "docs/memory/harness_reviewer_scorecard.yml"
```

Use the Write tool to create this file with exactly the content above.

- [ ] **Step 2: Verify YAML parses (render with placeholders)**

Run:
```bash
cd /Users/twelve/Music/myskills
sed -e 's/{{project_name}}/test-project/' \
    -e 's/{{project_type}}/nextjs/' \
    -e 's|{{root_fingerprint}}|package.json:name=test-project|' \
    harness-workflow/templates/project-memory/.harness-memory.yml.template | \
    python3 -c "import sys, yaml; d = yaml.safe_load(sys.stdin); assert d['schema_version'] == '1.0.0', d; assert d['forbidden_paths'], 'forbidden_paths must be non-empty'; print('OK')"
```

Expected output: `OK`

- [ ] **Step 3: Verify all required top-level keys present**

Run:
```bash
cd /Users/twelve/Music/myskills
grep -E "^(schema_version|project|owned_paths|forbidden_paths|errors_collection|archive_policy|reviewer):" harness-workflow/templates/project-memory/.harness-memory.yml.template | wc -l
```

Expected: `7`

- [ ] **Step 4: Commit**

Run:
```bash
cd /Users/twelve/Music/myskills
git add harness-workflow/templates/project-memory/.harness-memory.yml.template
git commit -m "feat(harness): add .harness-memory.yml.template with v1.0.0 schema"
```

---

## Task 3: Write `MEMORY.md.template`

**Files:**
- Create: `harness-workflow/templates/project-memory/MEMORY.md.template`

- [ ] **Step 1: Write the template**

Full content:

```markdown
# MEMORY.md

> Project long-term memory index. Shared between humans (free writing) and harness
> automation (inside `<!-- harness-memory:start -->` blocks only).
>
> **Rules for humans**: write anywhere OUTSIDE harness-memory blocks. harness will not
> touch your entries.
>
> **Rules for harness**: write ONLY inside your own `<!-- harness-memory:start id="..." -->`
> blocks. If you edit a user block, log to `.harness-memory.yml audits.conflicts` and stop.
>
> Spec reference: `docs/superpowers/specs/2026-04-22-memory-reviewer-upgrade.md`

## {{project_name}}

{{project_description}}

## Project Stack

<!-- harness-memory:start id="project-stack" schema="1.0.0" -->
- [Project Stack](harness_project_stack.md) — {{tech_stack_oneliner}}
- [Workflow](harness_workflow.md) — This project uses harness-workflow (8-Stage autonomous)
<!-- harness-memory:end id="project-stack" -->

## Team Preferences

<!-- users write freely below this line; harness will never touch it -->

<!-- e.g.:
- Commit messages use Conventional Commits, no emoji
- Prefer pnpm over npm
-->

## Recent Architecture Decisions

<!-- harness-memory:start id="recent-decisions" schema="1.0.0" -->
<!-- harness will append links to docs/memory/decisions/harness_*.md here as they accrue -->
<!-- harness-memory:end id="recent-decisions" -->

## Known Constraints

<!-- harness-memory:start id="constraints" schema="1.0.0" -->
<!-- harness will append links to docs/memory/constraints/harness_*.md here as they accrue -->
<!-- harness-memory:end id="constraints" -->
```

Use the Write tool. Placeholders: `{{project_name}}`, `{{project_description}}`, `{{tech_stack_oneliner}}`.

- [ ] **Step 2: Verify markers balanced + unique ids**

Run:
```bash
cd /Users/twelve/Music/myskills
F=harness-workflow/templates/project-memory/MEMORY.md.template
START=$(grep -c "<!-- harness-memory:start" $F)
END=$(grep -c "<!-- harness-memory:end" $F)
echo "start=$START end=$END"
# Extract ids and verify uniqueness
grep -oE 'id="[^"]+"' $F | sort | uniq -d
```

Expected:
```
start=3 end=3
```
And no duplicate ids (second command should print nothing).

- [ ] **Step 3: Commit**

Run:
```bash
cd /Users/twelve/Music/myskills
git add harness-workflow/templates/project-memory/MEMORY.md.template
git commit -m "feat(harness): add MEMORY.md.template with human+harness marker zones"
```

---

## Task 4: Write `ERRORS.md.template`

**Files:**
- Create: `harness-workflow/templates/project-memory/ERRORS.md.template`

- [ ] **Step 1: Write the template**

Full content:

```markdown
# ERRORS.md

> Error case library — each real bug that crossed the `errors_collection.min_criteria`
> threshold gets a dated file in `cases/`, indexed here.
>
> Purpose: future sessions grep this + `cases/*` by path/symbol/keyword before touching
> risky modules. A real scar beats a thousand principles.
>
> Spec reference: `docs/superpowers/specs/2026-04-22-memory-reviewer-upgrade.md`

## Index

<!-- harness-errors:start id="error-index" schema="1.0.0" -->
<!-- harness appends one line per case: - [<case-id>](cases/harness_<date>_<slug>.md) — <one-line summary> -->
<!-- harness-errors:end id="error-index" -->

## Writing a new case (for humans)

If you want to log a case manually (no `harness_` prefix), create it as
`cases/<your-slug>.md`. harness will grep-match it but never edit it.

Minimum threshold (for harness auto-collection): case must meet at least 2 of:
- Diagnosis took over 30 minutes
- Crossed module boundaries
- Recurred at least once
- Platform / browser / provider-specific
- Visible to end users or production
- Invalidated a prior assumption

## Reading cases (for agents)

Before editing code under `src/`, grep this file + `cases/*.md` by:
- Changed file path and module name
- Exported symbols you're about to modify
- Error messages from stack traces
- Browser / dependency names

Match types: `applies_to.paths` glob = strong; two weak signals = weak.
Output a `Memory check:` line in your response citing matches.
```

- [ ] **Step 2: Verify markers balanced**

Run:
```bash
cd /Users/twelve/Music/myskills
F=harness-workflow/templates/project-memory/ERRORS.md.template
echo "start=$(grep -c 'harness-errors:start' $F) end=$(grep -c 'harness-errors:end' $F)"
```

Expected: `start=1 end=1`

- [ ] **Step 3: Commit**

Run:
```bash
cd /Users/twelve/Music/myskills
git add harness-workflow/templates/project-memory/ERRORS.md.template
git commit -m "feat(harness): add ERRORS.md.template with case-library index"
```

---

## Task 5: Write four subdirectory `README.md` files

**Files:**
- Create: `harness-workflow/templates/project-memory/cases/README.md`
- Create: `harness-workflow/templates/project-memory/decisions/README.md`
- Create: `harness-workflow/templates/project-memory/constraints/README.md`
- Create: `harness-workflow/templates/project-memory/archive/README.md`

Note: These replace the `.gitkeep` files created in Task 1.

- [ ] **Step 1: Write `cases/README.md`**

Full content:

```markdown
# cases/

Each bug that clears the `errors_collection.min_criteria` threshold gets one file:
`harness_YYYY-MM-DD_<slug>.md` (harness-owned) or `<your-slug>.md` (human-owned, read-only for harness).

See the spec for full frontmatter schema — required keys: `id`, `date`, `module`,
`status`, `applies_to`, `criteria_met`, `freshness`, `next_time_signal`. Body must
include `## Negative Patterns` heading (content may be `(none)`).

Reference: `docs/superpowers/specs/2026-04-22-memory-reviewer-upgrade.md` §Error Case 文件格式
```

- [ ] **Step 2: Write `decisions/README.md`**

Full content:

```markdown
# decisions/

Architecture Decision Records emitted by Stage 2 planning. One file per decision:
`harness_YYYY-MM-DD_<slug>.md` (harness-owned) or `<your-slug>.md` (human-owned).

Suggested structure: Context → Options considered → Decision → Why → Consequences.
Cross-link to relevant cases in `cases/` and constraints in `constraints/` when they
drove the decision.

Harness picks up new decisions at Stage 8 and adds index entries to `MEMORY.md`
inside the `recent-decisions` marker block.
```

- [ ] **Step 3: Write `constraints/README.md`**

Full content:

```markdown
# constraints/

Legacy constraints, business rules, compliance requirements — facts that bound future
design choices but aren't decisions themselves.

Examples:
- "User sessions must not exceed 15 min due to finance compliance" → one file
- "Legacy OAuth flow must remain for 6 months during SSO migration" → one file

Files: `harness_<slug>.md` (harness-owned) or `<your-slug>.md` (human-owned).
No date required — constraints often persist longer than cases.
```

- [ ] **Step 4: Write `archive/README.md`**

Full content:

```markdown
# archive/

Cold storage for memory that is superseded, archived explicitly, or unused for
`archive_policy.archive_after_days_unused` days (default 180).

harness-owned: `harness_*.md` and `harness_*.yml` (scorecard rollover). User files
in `archive/` are read-only for harness.

Do not treat archived content as forgotten — a case referenced again after archival
gets re-promoted to active (freshness.state → active, last_used updated).
```

- [ ] **Step 5: Remove the `.gitkeep` placeholders**

Run:
```bash
cd /Users/twelve/Music/myskills
for sub in cases decisions constraints archive; do
  rm -f "harness-workflow/templates/project-memory/$sub/.gitkeep"
done
```

- [ ] **Step 6: Verify all four READMEs + no stray .gitkeep**

Run:
```bash
cd /Users/twelve/Music/myskills
find harness-workflow/templates/project-memory -name "README.md" | sort
find harness-workflow/templates/project-memory -name ".gitkeep"
```

Expected:
```
harness-workflow/templates/project-memory/archive/README.md
harness-workflow/templates/project-memory/cases/README.md
harness-workflow/templates/project-memory/constraints/README.md
harness-workflow/templates/project-memory/decisions/README.md
```
(And `.gitkeep` search prints nothing.)

- [ ] **Step 7: Commit**

Run:
```bash
cd /Users/twelve/Music/myskills
git add harness-workflow/templates/project-memory/
git commit -m "feat(harness): add README.md for cases/decisions/constraints/archive subdirs"
```

---

## Task 6: Rewrite `harness-workflow/references/memory.md` (total rewrite)

**Files:**
- Modify: `harness-workflow/references/memory.md` — note this file currently does NOT exist (spec says "大改" but after Round 1 回滚, it was never committed). Create from scratch.

Content source: directly lift and consolidate from spec sections §`.harness-memory.yml` Contract + §`docs/memory/` 物理形态 + §Runtime 协议.

- [ ] **Step 1: Write the full content**

Target file: `/Users/twelve/Music/myskills/harness-workflow/references/memory.md`

Required section headers (in order):

```
# harness-workflow Memory Runtime Reference

> 本文档是 harness-workflow 内项目长期记忆机制的权威 runtime 规范。
> 完整设计推导见 specs/2026-04-22-memory-reviewer-upgrade.md。

## 1. 两类记忆的分工
## 2. `.harness-memory.yml` Contract
### 2.1 Schema
### 2.2 版本演化 (semver patch/minor/major)
### 2.3 失败处理
### 2.4 硬约束 (forbidden_paths 必填 + broad 模式拒绝 + 逃生门)
## 3. `docs/memory/` 物理形态
### 3.1 目录布局
### 3.2 前缀规则 (harness_ vs 用户)
### 3.3 HTML Marker 协议
### 3.4 Error Case 文件格式
## 4. Runtime 协议
### 4.1 ERRORS.md 查询 (Stage 3 前置)
### 4.2 执法点 (四步：baseSha / 预查 / 注入 / post-check / 实施后扫描)
### 4.3 Suspect Rule 文法
### 4.4 归档 + last_used 更新规则
## 5. 生命周期三时机
### 5.1 --init / --adopt (scaffold)
### 5.2 Stage 8 收尾刷新
### 5.3 --maintain 漂移检查
## 6. Red Flags — 不要存什么
```

Content for each section: **directly copy from spec**:
- §2.1 from spec lines 104-167
- §2.2 from spec lines 169-177
- §2.3 from spec lines 179-187
- §2.4 from spec lines 189-196
- §3.1 from spec lines 204-220
- §3.2 from spec lines 222-225
- §3.3 from spec lines 227-242
- §3.4 from spec lines 246-295
- §4.1 from spec lines 303-321 + `**硬门**` line 323
- §4.2 from spec lines 325-399
- §4.3 from spec lines 403-418
- §4.4 from spec line 295 (`freshness.last_used` rule) + spec lines 150-154 (`archive_policy`)
- §5 synthesize from spec Resolved Decisions and spec §实施范围 / §验证方式
- §6 copy Red Flags from harness-workflow/references/maintenance.md table (existing content, lines 41-47)

Use the Write tool to create this file. The file will be ~350 lines.

- [ ] **Step 2: Verify required sections exist**

Run:
```bash
cd /Users/twelve/Music/myskills
F=harness-workflow/references/memory.md
grep -c "^## " $F
grep -E "^## (1\.|2\.|3\.|4\.|5\.|6\.)" $F
```

Expected: `grep -c` prints `6` (six H2 sections); second command prints the six section headers.

- [ ] **Step 3: Verify cross-references to spec are valid**

Run:
```bash
cd /Users/twelve/Music/myskills
grep -E "specs/2026-04-22-memory-reviewer-upgrade" harness-workflow/references/memory.md | head -3
ls docs/superpowers/specs/2026-04-22-memory-reviewer-upgrade.md
```

Expected: spec path referenced in memory.md at least once; `ls` prints the spec file.

- [ ] **Step 4: Verify no orphan `{hash}` or `~/.claude/projects` leftovers from the (rolled-back) Round 1**

Run:
```bash
cd /Users/twelve/Music/myskills
grep -n '{hash}\|~/\.claude/projects' harness-workflow/references/memory.md || echo "CLEAN"
```

Expected: `CLEAN`

- [ ] **Step 5: Commit**

Run:
```bash
cd /Users/twelve/Music/myskills
git add harness-workflow/references/memory.md
git commit -m "feat(harness): memory.md as contract runtime reference

Consolidates .harness-memory.yml schema, docs/memory/ layout, HTML markers,
error case format, runtime query protocol, Stage 3 enforcement (4 steps),
suspect grammar, last_used update rule, and lifecycle three-timing (--init /
Stage 8 / --maintain). Source: specs/2026-04-22-memory-reviewer-upgrade.md"
```

---

## Task 7: Write `harness-workflow/references/memory-migrations.md`

**Files:**
- Create: `harness-workflow/references/memory-migrations.md`

- [ ] **Step 1: Write the file**

Full content:

```markdown
# `.harness-memory.yml` Schema Migrations

> Declarative migration steps per major version bump. Harness reads this file on
> `--init/--adopt/--maintain` when it detects a contract whose `schema_version`
> is older than the current release.
>
> Reference: `specs/2026-04-22-memory-reviewer-upgrade.md` §版本演化

## Format

Each migration is a section keyed by semver range. Steps are declarative (natural
language + YAML diffs), not executable code. Harness's job is to read, explain the
change to the user, and apply only after explicit confirmation.

## Current Version

`1.0.0` — Initial release (this commit).

No prior versions exist, so no migration steps are defined yet.

## Future: 1.0.0 → 2.0.0 (placeholder for next major bump)

When schema_version 2.0.0 is released, a section like this will be appended:

```
### 1.0.0 → 2.0.0

**Scope of change:**
- <describe what changed in ownership / field names / behavior>

**Breaking:**
- <list breaking changes>

**Migration steps:**
1. Back up current `.harness-memory.yml` to `archive/harness-memory-v1.yml`
2. Rename fields: <old> → <new>
3. Re-validate with new schema
4. Update schema_version to "2.0.0"

**User action required:**
- Confirm YES/NO after harness shows diff preview
```

## Rules

- **Patch (x.y.z bump)**: no migration needed
- **Minor (x.Y.z bump)**: optional; harness reads new fields with defaults
- **Major (X.y.z bump)**: migration section REQUIRED in this file before release
- **No migration for downgrades** — harness refuses to operate on newer-major contract in read-only mode
```

- [ ] **Step 2: Verify file exists and parses**

Run:
```bash
cd /Users/twelve/Music/myskills
ls harness-workflow/references/memory-migrations.md
grep "^## Current Version" harness-workflow/references/memory-migrations.md
```

Expected: file exists, "Current Version" header present.

- [ ] **Step 3: Commit**

Run:
```bash
cd /Users/twelve/Music/myskills
git add harness-workflow/references/memory-migrations.md
git commit -m "feat(harness): add memory-migrations.md with v1.0.0 baseline"
```

---

# Phase B — `strict-reviewer` skill + invocation doc

## Task 8: Create `strict-reviewer/SKILL.md`

**Files:**
- Create: `strict-reviewer/SKILL.md`

This is a NEW top-level skill directory at `/Users/twelve/Music/myskills/strict-reviewer/`.

- [ ] **Step 1: Create directory**

Run:
```bash
cd /Users/twelve/Music/myskills
mkdir -p strict-reviewer
```

- [ ] **Step 2: Write `SKILL.md`**

Target file: `/Users/twelve/Music/myskills/strict-reviewer/SKILL.md`

Full content:

```markdown
---
name: strict-reviewer
description: 反谄媚审稿 skill — 默认 FAIL 立场 + 三硬门（Grounding / Reproduction / Coverage）+ Minimum adversarial search。Schema-driven 薄 wrapper，stateless。用于 PR 审查、QA、安全审查、spec 审查等任何需要严格证据链的审稿场景。harness-workflow Stage 4/5/6/7 自动调用。手动调用：/strict-reviewer <review_target YAML>。Triggers：(1) "严格审稿 / strict review" (2) harness-workflow reviewer 阶段 (3) 用户直接调 /strict-reviewer
---

# strict-reviewer — 反谄媚审稿 Skill

> 薄 wrapper。**不是** persona skill。Schema driven。Stateless。
>
> 设计参考：`/Users/twelve/Music/myskills/harness-workflow/specs/2026-04-22-memory-reviewer-upgrade.md` §`strict-reviewer` Skill — IO Contract

## 默认立场（硬编码在 prompt 前置）

```
DEFAULT = FAIL. Hesitation counts as FAIL.
You are not here to be kind. You are here to find what will break.
PASS is a privilege that requires all three gates + three adversarial mode
analyses to pass cleanly.
If coverage is incomplete, verdict is BLOCKED — not PASS.
```

这不是自我激励辞，是**契约**。违反任一硬门的 PASS 等同于 false pass，未来会被 false-pass-correction 机制追溯。

## Input（caller 必须传入的 YAML 字符串）

```yaml
review_target:
  changed_files:          # required, array of strings
    - "src/auth/session.ts"
  diff_summary: "..."     # required, string
  stage: "qa"             # required, enum: qa | security | spec | quality
  claims_to_verify:       # optional, array of strings
    - "code handles expired refresh cookie"
  memory_cases: []        # optional, array — from Stage 3 .harness-status.json.memoryCheck.matches
  prior_verdict: null     # optional — if this is a re-review after FAIL
```

若 caller 未提供 required 字段 → 立即返回 `verdict: BLOCKED` 原因 `missing required input field: <field>`。

## Required Steps（严格执行，不跳）

1. **Read every changed file** — 用 Read tool 读每个 `changed_files` 文件。不能读的 → 加入 `coverage.skipped_files` 并说明原因
2. **Verify every claim** — 对 `claims_to_verify` 的每一条，对着 file:line / 命令输出 / repro 步骤逐条验证。没证据支持的 claim 算 finding（severity: high，grounded_by: 缺席）
3. **Minimum adversarial search** — 列出 **3 个**可能失败模式（concurrent race / edge input / auth bypass / data corruption / resource leak 等）。对每一条，论证：
   - 当前代码是否处理了
   - 若未处理但 severity 低 → low finding
   - 若未处理且 severity 高 → high/critical finding
   不列够 3 个 = 审稿未完成，返回 BLOCKED
4. **Apply three gates**:
   - **Grounding gate**：每个 finding 必须带 `file:line` 或 `符号` 或 `命令输出` 或 `repro 步骤`。纯口头 finding 不计数
   - **Reproduction gate**：任何声称的 bug 必须有 repro steps / failing test / trace，或者 `reproduction: "not reproducible because <reason>"`
   - **Coverage gate**：`coverage.inspected_files` 必须包含所有 `changed_files` 里的关键文件（非测试文件、非 generated）。未读关键变更文件 → verdict 不得 PASS，应为 BLOCKED

## Output（严格 YAML，供 caller 解析）

```yaml
verdict: "PASS" | "FAIL" | "BLOCKED"
reasons:
  - "adversarial search hit #2: expired token replay"
coverage:
  inspected_files:
    - "src/auth/session.ts"
  skipped_files:
    - path: "src/auth/types.ts"
      reason: "type-only, no runtime logic"
adversarial_search:
  failure_modes_checked:
    - "concurrent refresh race"
    - "expired token replay missing nonce check"
    - "CSRF via subdomain cookie leak"
  hits:
    - "#2 — expired token replay missing nonce check"
findings:
  - severity: "high"
    file: "src/auth/session.ts"
    line: 47
    grounded_by: "read:file"
    reproduction: "test/auth/session.spec.ts:expired-refresh-case"
    message: "refresh path does not invalidate old session token"
scorecard_delta:
  total_reviews: 1
  pass_count: 0
  fail_count: 1
  blocked_count: 0
```

### Verdict 决定规则

| 条件 | verdict |
|------|---------|
| 至少一个 `critical` finding | **FAIL** |
| 至少一个 `high` finding 且 stage ∈ {qa, security, spec} | **FAIL** |
| `high` finding 只在 stage=quality | **FAIL**（quality stage 也严格）|
| 仅 `medium` findings | **FAIL**（但 caller 可接受为 "merge-with-knownissues"）|
| 仅 `low` findings + 三硬门全过 + adversarial 3 条全论证 | **PASS** |
| 任何硬门失败 | **BLOCKED** |
| Input malformed / 缺 required 字段 | **BLOCKED** |

## Scorecard（caller 负责 persist）

`strict-reviewer` 自己 **完全 stateless**。`scorecard_delta` 是给 caller 的一次性 payload。

Caller（harness Stage 6/7 coordinator，或手动调用用户）按 `docs/memory/harness_reviewer_scorecard.yml` 的 schema（见 spec §`harness_reviewer_scorecard.yml` Schema）做：
1. Append 一条 `reviews[]` entry
2. 更新 `totals` 各计数
3. 若超过 500 条 → 归档到 `docs/memory/archive/harness_reviewer_scorecard_<year>.yml` + 保留最新 100 条

## 何时被调用

### 自动（harness-workflow 内部）

`harness-workflow/references/workflow.md` 定义 Stage 4/5/6/7 在完成各自工作后，由主 agent coordinator 构造 `review_target` + invoke `strict-reviewer`。

详细协议 → `harness-workflow/references/reviewer-integration.md`

### 手动（用户直接用）

```
/strict-reviewer <review_target YAML>
```

独立模式下 scorecard 无处写 → 输出的 `scorecard_delta` 返给用户，用户自己决定存哪。

## 不该做什么（避免 persona 膨胀）

- ❌ 不写 "I am a brutal senior engineer..." 之类 persona prose
- ❌ 不根据 stage 改变"人格"（qa 和 security 的域不同，但硬门和 adversarial search 流程一致）
- ❌ 不自己读 `docs/memory/` 或 `.harness-memory.yml`（caller 已在 `memory_cases` 注入）
- ❌ 不跨 review 持 state（stateless 铁律）
- ❌ 不降级到 "temporary relaxed mode"。若资源不够 → BLOCKED
```

Use the Write tool to create this skill file.

- [ ] **Step 3: Verify SKILL.md frontmatter is valid**

Run:
```bash
cd /Users/twelve/Music/myskills
head -3 strict-reviewer/SKILL.md
# Parse frontmatter
python3 -c "
import yaml
content = open('strict-reviewer/SKILL.md').read()
parts = content.split('---', 2)
fm = yaml.safe_load(parts[1])
assert 'name' in fm and fm['name'] == 'strict-reviewer', fm
assert 'description' in fm and len(fm['description']) > 100, len(fm.get('description',''))
print('OK')
"
```

Expected:
- `head -3` shows `---` / `name: strict-reviewer` / `description: ...`
- Python output: `OK`

- [ ] **Step 4: Verify required sections present**

Run:
```bash
cd /Users/twelve/Music/myskills
for section in "默认立场" "Input" "Required Steps" "Output" "Verdict 决定规则" "Scorecard" "何时被调用"; do
  grep -c "$section" strict-reviewer/SKILL.md | head -1 | grep -q "^[1-9]" && echo "OK: $section" || echo "MISSING: $section"
done
```

Expected: all OK.

- [ ] **Step 5: Symlink into `~/.claude/skills/` so Claude can see it**

Run:
```bash
ln -sf /Users/twelve/Music/myskills/strict-reviewer ~/.claude/skills/strict-reviewer
readlink ~/.claude/skills/strict-reviewer
```

Expected: prints `/Users/twelve/Music/myskills/strict-reviewer`

- [ ] **Step 6: Commit**

Run:
```bash
cd /Users/twelve/Music/myskills
git add strict-reviewer/
git commit -m "feat: add strict-reviewer skill — default-FAIL three-gate reviewer

Thin wrapper, schema-driven, stateless. Enforces grounding / reproduction /
coverage gates + minimum adversarial search (3 failure modes). Callable by
harness-workflow Stage 4/5/6/7 or directly via /strict-reviewer. Spec:
docs/superpowers/specs/2026-04-22-memory-reviewer-upgrade.md"
```

---

## Task 9: Write `harness-workflow/references/reviewer-integration.md`

**Files:**
- Create: `harness-workflow/references/reviewer-integration.md`

- [ ] **Step 1: Write the file**

Full content:

```markdown
# harness-workflow ↔ strict-reviewer Integration

> Caller-side protocol for Stage 4/5/6/7 to invoke `strict-reviewer` correctly.
> Spec reference: `specs/2026-04-22-memory-reviewer-upgrade.md` §Invocation Protocol

## Roles

- **strict-reviewer skill** — stateless reviewer (see `strict-reviewer/SKILL.md`)
- **Caller** — harness-workflow main agent acting as Stage 4/5/6/7 coordinator
- **Scorecard** — `docs/memory/harness_reviewer_scorecard.yml`, owned and persisted by caller

## Protocol

### 1. Construct `review_target`

Caller gathers inputs from current round state:

| Field | Source |
|-------|--------|
| `changed_files` | `git diff --name-only <.harness-status.json.baseSha>..HEAD` |
| `diff_summary` | `git log --oneline <baseSha>..HEAD` + `git diff --stat` |
| `stage` | Current Stage: `"qa"` for Stage 6, `"security"` for Stage 7, `"spec"` for Stage 4, `"quality"` for Stage 5 |
| `claims_to_verify` | Stage-specific: Stage 4 = spec acceptance criteria; Stage 5 = plan's test checklist; Stage 6 = PRD acceptance criteria; Stage 7 = security requirements |
| `memory_cases` | Read from `.harness-status.json.memoryCheck.matches` |
| `prior_verdict` | If this is a re-review after FAIL, pass the prior output; else null |

### 2. Invoke

```
Skill(skill="strict-reviewer", args=<serialized review_target YAML>)
```

Caller waits for YAML-shaped response.

### 3. Parse response

Response is YAML matching the Output schema in `strict-reviewer/SKILL.md`. Caller MUST:

- Parse as YAML (Python `yaml.safe_load` or equivalent)
- If parse fails → retry once with identical `review_target`
- If second parse fails → treat as `verdict: BLOCKED` reason `"reviewer output malformed"`

### 4. Route by verdict

| verdict | caller action |
|---------|--------------|
| `PASS` | Continue to next Stage. Append `scorecard_delta` to scorecard (see step 5). |
| `FAIL` | Enter Stage's auto-fix loop: Stage 4 ≤ 3 rounds / Stage 5 ≤ 3 rounds / Stage 6/7 per P0 bug rules. Each retry builds a new `review_target` with `prior_verdict` set. |
| `BLOCKED` | **Escalate to user**. Do not auto-retry, do not continue. `BLOCKED` means system state is incomplete (input malformed, coverage impossible, etc.), not that code is broken. |

### 5. Scorecard persistence (caller owns, strict-reviewer is stateless)

For every invocation (PASS / FAIL / BLOCKED), caller appends to `docs/memory/harness_reviewer_scorecard.yml`:

```yaml
reviews:
  - review_id: "r-<date>-<N>"            # generate monotonic id per round
    stage: "<stage>"
    timestamp: "<ISO>"
    changed_files: [...]
    verdict: "<verdict>"
    findings_count: <from output>
    linked_error_case: null              # set later if false-pass correction fires
```

And update `totals`:

```yaml
totals:
  total_reviews: +1
  <verdict lower>_count: +1
```

Then check rotation: if `len(reviews) > 500` → move older entries to `docs/memory/archive/harness_reviewer_scorecard_<year>.yml` (keep most recent 100).

## strict-reviewer unavailable (degraded mode)

If `Skill(skill="strict-reviewer", ...)` fails (skill missing, tool error, timeout):

| Context | Action |
|---------|--------|
| harness mode (docs/STATE.json exists) | **BLOCKED**, escalate. NEVER fall back to legacy prompt. |
| Standalone use (no STATE.json) | Fall back to legacy qa-prompt / security-prompt; log knownIssue |
| User explicitly approves one-time degrade | Legacy prompt for this invocation only; append knownIssue with reason |
| `autonomous_mode` | Always BLOCKED, wait for user. Do not silently degrade. |

Rationale: the whole point of strict-reviewer is the hard gates. When the enforcement infrastructure is gone is exactly when you need the gates most, not least.

## Linking false-pass incidents

When a later bug contradicts a prior PASS review (the feedback loop that makes scorecard worth having):

1. Open a new error case in `docs/memory/cases/harness_<date>_<slug>.md` (if it meets `errors_collection.min_criteria`)
2. Append to scorecard `false_pass_incidents`:
   ```yaml
   false_pass_incidents:
     - incident_id: "fpi-<date>-<N>"
       original_review_id: "r-<date>-<N>"   # the review that missed this
       bug_case: "cases/harness_<date>_<slug>.md#<id>"
       detected_at: "<ISO>"
       action_taken: "..."
   ```
3. `--maintain` scans `false_pass_incidents` periodically to feed reviewer-prompt examples (future work)

## Claims verification guidance per stage

Callers should pick `claims_to_verify` appropriate to the stage:

- **Stage 4 (spec review)**: each acceptance criterion in plan / spec — does the code meet it?
- **Stage 5 (quality / codex cross-review)**: core architecture assertions — single responsibility, no layering violations, no abandoned branches
- **Stage 6 (QA)**: each test case in plan's test checklist — does the code + test pair produce the expected behavior?
- **Stage 7 (security)**: OWASP-style threat statements — does the code prevent <specific class of attack>?

Empty `claims_to_verify` is allowed but strongly discouraged — it lets strict-reviewer only rely on adversarial search, reducing signal density.
```

- [ ] **Step 2: Verify sections present**

Run:
```bash
cd /Users/twelve/Music/myskills
F=harness-workflow/references/reviewer-integration.md
grep -c "^## " $F
grep -c "strict-reviewer" $F
```

Expected: `^## ` count ≥ 5; `strict-reviewer` count ≥ 10.

- [ ] **Step 3: Commit**

Run:
```bash
cd /Users/twelve/Music/myskills
git add harness-workflow/references/reviewer-integration.md
git commit -m "feat(harness): add reviewer-integration.md caller protocol"
```

---

# Phase C — harness-workflow skill integration

## Task 10: Update `harness-workflow/skill.md` — Phase 3 + Stage 8 checklist

**Files:**
- Modify: `harness-workflow/skill.md` (lines 139-146 Phase 3; around Stage 8 checklist if present)

- [ ] **Step 1: Read current Phase 3 + check for Stage 8 checklist**

Run:
```bash
cd /Users/twelve/Music/myskills
sed -n '135,160p' harness-workflow/skill.md
grep -n "自检\|Stage 8" harness-workflow/skill.md
```

Note the exact current text of the Phase 3 block.

- [ ] **Step 2: Replace Phase 3 with contract init version**

Use Edit tool on `harness-workflow/skill.md`.

Replace the existing Phase 3 block (starting at `## Phase 3: 记忆与审查`, ending at the `---` before `## Phase 4`) with:

```markdown
## Phase 3: 项目记忆契约初始化

> 走 `--init` 或 `--adopt` 时执行。`--maintain` 不重跑（只做漂移检查）。

1. **生成 `.harness-memory.yml`**（contract 锚点）

   从 `templates/project-memory/.harness-memory.yml.template` 渲染，填充：
   - `project.name` (from `package.json.name` / `go.mod` / `pyproject.toml`)
   - `project.type` (from `.harness-context.json.projectType`)
   - `project.root_fingerprint` (格式：`package.json:name=<value>` 或同类锚点)

   写入 `docs/memory/.harness-memory.yml`。

2. **生成 `docs/memory/` 骨架**

   从 `templates/project-memory/` 复制：
   - `MEMORY.md.template` → `docs/memory/MEMORY.md`（渲染 `{{project_name}}` / `{{project_description}}` / `{{tech_stack_oneliner}}`）
   - `ERRORS.md.template` → `docs/memory/ERRORS.md`
   - 四个子目录 `cases/` / `decisions/` / `constraints/` / `archive/` + 各自 `README.md`

3. **初始化 scorecard**（`docs/memory/harness_reviewer_scorecard.yml`）

   ```yaml
   schema_version: "1.0.0"
   totals:
     total_reviews: 0
     pass_count: 0
     fail_count: 0
     blocked_count: 0
   reviews: []
   false_pass_incidents: []
   ```

4. **`--adopt` 合并保护**：如目标项目已有 `docs/memory/`，只创建缺失文件，**绝不覆盖用户文件**，MEMORY.md / ERRORS.md 里只添加缺失 `harness-memory:start` / `harness-errors:start` 标记块。

5. **契约验证**

   - `forbidden_paths` 非空（spec §硬约束）
   - `owned_paths` 无 broad unscoped 模式
   - YAML parse 无异常

   任一失败 → BLOCKED，停止 `--init`，要求用户确认。

**完整 runtime 规范** → 见 [references/memory.md](references/memory.md)
**Schema 迁移策略** → 见 [references/memory-migrations.md](references/memory-migrations.md)
**Reviewer 调用协议** → 见 [references/reviewer-integration.md](references/reviewer-integration.md)
```

- [ ] **Step 3: Add Stage 8 self-check items**

Find the Stage 8 checklist block in `harness-workflow/skill.md` (if present), and add these items (if no block exists, skip):

```markdown
- [ ] `.harness-memory.yml` contract 通过验证（forbidden_paths 非空 + 无 broad 模式）
- [ ] `harness_project_stack.md` 反映当前技术栈（Stage 8 刷新）
- [ ] Scorecard 追加了本轮所有 review 条目（无遗漏）
- [ ] 本轮新 case（若 errors_collection 阈值达成）已写入 `docs/memory/cases/harness_<date>_<slug>.md`
- [ ] `MEMORY.md` 索引 marker 块内已追加新决策 / 案例链接
```

Use the Edit tool. If the Stage 8 section doesn't explicitly list checklist items, append the section:

```markdown
## Stage 8 收尾自检（memory + reviewer 相关）

- [ ] `.harness-memory.yml` contract 通过验证（forbidden_paths 非空 + 无 broad 模式）
- [ ] `harness_project_stack.md` 反映当前技术栈
- [ ] Scorecard 追加了本轮所有 review 条目
- [ ] 本轮新 case（若达门槛）已写入 `docs/memory/cases/`
- [ ] `MEMORY.md` 索引 marker 块内已追加新链接
```

- [ ] **Step 4: Verify edits took**

Run:
```bash
cd /Users/twelve/Music/myskills
grep -c "\.harness-memory\.yml" harness-workflow/skill.md
grep -c "strict-reviewer\|reviewer-integration" harness-workflow/skill.md
```

Expected: both counts ≥ 1.

- [ ] **Step 5: Verify no broken cross-refs**

Run:
```bash
cd /Users/twelve/Music/myskills
# Every reference mentioned in Phase 3 should exist
for f in harness-workflow/references/memory.md \
         harness-workflow/references/memory-migrations.md \
         harness-workflow/references/reviewer-integration.md; do
  test -f $f && echo "OK: $f" || echo "MISSING: $f"
done
```

Expected: all three OK.

- [ ] **Step 6: Commit**

Run:
```bash
cd /Users/twelve/Music/myskills
git add harness-workflow/skill.md
git commit -m "feat(harness): skill.md Phase 3 rewrites to contract init + Stage 8 memory checklist"
```

---

## Task 11: Update `harness-workflow/references/workflow.md` — Stage 2/3/6/7/8 hooks

**Files:**
- Modify: `harness-workflow/references/workflow.md`

This is the biggest single-file change. Five stages to update.

- [ ] **Step 1: Read current state of workflow.md**

Run:
```bash
cd /Users/twelve/Music/myskills
wc -l harness-workflow/references/workflow.md
grep -n "^## Stage" harness-workflow/references/workflow.md
```

Record line ranges for each Stage.

- [ ] **Step 2: Update Stage 2 — add baseSha capture + decision memory**

Use Edit tool. After Stage 2's existing body, before the `---` separator, append:

```markdown
### Stage 2 memory hooks

**1. Capture baseSha (mandatory, BEFORE dispatching Stage 3)**

Before Stage 3 subagent dispatch, coordinator MUST:

```bash
BASE_SHA=$(git rev-parse HEAD)
# Write to .harness-status.json.baseSha
python3 -c "
import json
s = json.load(open('.harness-status.json'))
s['baseSha'] = '$BASE_SHA'
s['baseCapturedAt'] = '$(date -u +%Y-%m-%dT%H:%M:%SZ)'
json.dump(s, open('.harness-status.json','w'), indent=2)
"
```

This SHA is used by Stage 4 post-diff scan to detect plan-vs-actual file drift. If `.harness-status.json.baseSha` is missing when Stage 4 starts → BLOCKED.

**2. Emit decision records (if plan involves architectural choice)**

If Stage 2 plan contains `## Architecture Decisions` or equivalent, write one file per decision to `docs/memory/decisions/harness_<YYYY-MM-DD>_<slug>.md`. Stage 8 will index them in `MEMORY.md`.
```

- [ ] **Step 3: Update Stage 3 — ERRORS precheck + enforcement**

Append to Stage 3:

```markdown
### Stage 3 ERRORS.md enforcement (four-step)

See `references/memory.md` §4.2 for full specification. Summary:

**Step 1 — Coordinator precheck** (BEFORE dispatching each Stage 3 subagent task):

For each file in plan's `changed_files`, grep `docs/memory/ERRORS.md` + `docs/memory/cases/*.md` by:
- Full path
- Module name
- Basename
- Exported symbols

Write results to `.harness-status.json.memoryCheck`:
```json
{
  "queriedAt": "<ISO>",
  "queriedFiles": ["src/auth/session.ts", ...],
  "matches": [
    { "file": "...", "case_id": "...", "relevance": "strong|weak", "action": "..." }
  ]
}
```

**Step 2 — Inject into subagent task prompt**:

Each Stage 3 subagent task prompt prepended with:
```
# Memory Context (by coordinator precheck)
Relevant ERRORS cases:
- <case-id> — <reason> / relevance=<strong|weak> → before coding, <action>

(or "No relevant ERRORS cases found.")

You MUST echo a "Memory check:" line in your output, and for strong relevance,
provide implementation evidence (file:line or test:case) that you performed <action>.
```

**Step 3 — Post-check by relevance** (after each subagent output):

- Missing "Memory check:" line → BLOCKED, retry once; second miss → escalate
- Has "Memory check:" line, but STRONG relevance action has no evidence (grep for `file:line` or `test:case` in output) → BLOCKED, retry once
- Has "Memory check:" line, WEAK relevance action has no evidence → log to knownIssues, do not block

**Step 4 — Post-implementation diff scan** (after Stage 3 completes, before Stage 4):

```bash
BASE_SHA=$(jq -r .baseSha .harness-status.json)
git diff --name-only $BASE_SHA..HEAD > /tmp/actual-diff.txt
jq -r '.memoryCheck.queriedFiles[]' .harness-status.json > /tmp/queried.txt

# Files in actual-diff but NOT in queried.txt
comm -23 <(sort /tmp/actual-diff.txt) <(sort /tmp/queried.txt)
```

For each unchecked file: re-run ERRORS query.
- Strong match → generate remediation task OR escalate to user
- Weak match → append to `matches`, log knownIssue, Stage 4 may proceed
- No match → no action

Stage 4 entrance gate: `queriedFiles` must cover all files in actual diff. Else BLOCKED.
```

- [ ] **Step 4: Update Stage 4 — invoke strict-reviewer**

Replace Stage 4's existing prompt reference (if any) with:

```markdown
### Stage 4 reviewer invocation

Stage 4 is "spec review" — verify implementation matches plan / spec acceptance criteria.

Coordinator constructs `review_target` per `references/reviewer-integration.md`:
- `stage: "spec"`
- `claims_to_verify`: acceptance criteria from plan
- `memory_cases`: from `.harness-status.json.memoryCheck.matches`
- `changed_files`: from `git diff --name-only <baseSha>..HEAD`

Invoke:
```
Skill(skill="strict-reviewer", args=<review_target YAML>)
```

Route verdict per `reviewer-integration.md` §4. Append scorecard_delta per §5.

Legacy `prompts/spec-prompt.md` (if exists) deprecated — keep for reference only.
```

- [ ] **Step 5: Update Stage 5 — invoke strict-reviewer for quality**

Similar structure to Stage 4 but with `stage: "quality"` and codex cross-model as parallel check.

- [ ] **Step 6: Update Stage 6 — invoke strict-reviewer (QA) replacing qa-prompt**

Update text mentioning `prompts/qa-prompt.md` to:

```markdown
### Stage 6 QA via strict-reviewer

Stage 6 QA coordinator calls strict-reviewer (not legacy qa-prompt):

```
review_target:
  stage: "qa"
  claims_to_verify: [PRD acceptance criteria + plan's test checklist]
  memory_cases: [from .harness-status.json.memoryCheck.matches]
  changed_files: [from git diff --name-only <baseSha>..HEAD]
```

Route by verdict (see reviewer-integration.md). For FAIL:
- Build failing test from `findings[].reproduction` per row
- Patch code
- Re-invoke strict-reviewer with same `review_target` + `prior_verdict`
- Max 3 rounds, then escalate

Legacy `prompts/qa-prompt.md` kept as domain-context reference (passed as part of claims_to_verify hint), no longer the top-level prompt.
```

- [ ] **Step 7: Update Stage 7 — same pattern with `stage: "security"`**

- [ ] **Step 8: Update Stage 8 — memory refresh + post-diff**

Append:

```markdown
### Stage 8 memory refresh

Before git commit of the round:

1. **Refresh harness_project_stack.md**: regenerate from current `.harness-context.json`
2. **Index new decisions**: scan `docs/memory/decisions/harness_*.md` files newer than previous round, add links inside `MEMORY.md` marker block `id="recent-decisions"`
3. **Index new cases**: scan `docs/memory/cases/harness_*.md` files newer than previous round, add index entries inside `ERRORS.md` marker block `id="error-index"`
4. **Update freshness.last_used** on harness-owned cases that were matched during this round (from `.harness-status.json.memoryCheck.matches`)
5. **Check suspect rules**: for each `suspect_rules[]` entry, if its `applies_to` matched this round's changes, mark referenced cases `freshness.state: suspect` + `suspect_since: <ISO>`
6. **Archive policy check**: scan all harness-owned cases, if `freshness.last_used` older than `archive_policy.archive_after_days_unused` → move to `docs/memory/archive/harness_<date>_<slug>.md` + update `status: archived`
7. **Scorecard rotation**: if `reviews[]` length > 500, move older to `archive/harness_reviewer_scorecard_<year>.yml`, keep latest 100

After this, proceed with git commit.
```

- [ ] **Step 9: Verify all five stages updated**

Run:
```bash
cd /Users/twelve/Music/myskills
F=harness-workflow/references/workflow.md
for stage_num in 2 3 4 5 6 7 8; do
  grep -c "Stage $stage_num" $F | head -1
done
grep -c "strict-reviewer" $F
grep -c "baseSha\|memoryCheck\|memory\.md\|memory refresh" $F
```

Expected: `strict-reviewer` count ≥ 4 (Stage 4/5/6/7); memory terms count ≥ 5.

- [ ] **Step 10: Commit**

Run:
```bash
cd /Users/twelve/Music/myskills
git add harness-workflow/references/workflow.md
git commit -m "feat(harness): workflow.md integrates memory + strict-reviewer hooks

Stage 2: baseSha capture + decisions emitted
Stage 3: 4-step ERRORS enforcement (precheck / inject / post-check / post-diff scan)
Stage 4/5/6/7: strict-reviewer invocation (replaces direct prompt usage)
Stage 8: memory refresh (stack / decisions / cases / last_used / suspect / archive / scorecard rotation)"
```

---

## Task 12: Update `harness-workflow/references/maintenance.md`

**Files:**
- Modify: `harness-workflow/references/maintenance.md`

- [ ] **Step 1: Read current state**

Run:
```bash
cd /Users/twelve/Music/myskills
cat harness-workflow/references/maintenance.md
```

- [ ] **Step 2: Add `--maintain` memory audit section**

Append to end of file:

```markdown

## `--maintain` memory audit (new in v1.0.0)

Every `--maintain` invocation runs contract-level drift checks on the target project's `docs/memory/`:

### 1. Contract validation
- Load `.harness-memory.yml`
- Fail closed if malformed → `BLOCKED`, report to user
- Validate hard constraints (forbidden_paths non-empty, no broad unscoped owned_paths, schema_version compatible)

### 2. HTML marker conflicts review
- For each `MEMORY.md` / `ERRORS.md` file, find `<!-- harness-memory:start -->` / `<!-- harness-errors:start -->` blocks
- If any block contents differ from harness's last known state (stored in `audits.conflicts`), surface to user
- User chooses: keep edit (remove from conflicts) or revert to harness version

### 3. Suspect detection
- For every `suspect_rules[]` in contract:
  - Compute `git diff --name-only HEAD~30..HEAD` (last 30 commits as "recent change window")
  - If any `applies_to` fires, mark referenced cases `freshness.state: suspect`

### 4. Archive sweep
- Scan `docs/memory/cases/harness_*.md`
- If `freshness.last_used` older than `archive_policy.archive_after_days_unused` (default 180) → move to `archive/`

### 5. Scorecard rotation
- If `docs/memory/harness_reviewer_scorecard.yml` has > 500 reviews → rotate older entries to `archive/harness_reviewer_scorecard_<year>.yml`

### 6. Audit timestamp update
- Update `audits.last_full_audit` / `audits.last_error_audit` / `audits.last_reviewer_score_audit` in `.harness-memory.yml` per the subtask that ran

Full runtime details → `references/memory.md` §5.3.
```

- [ ] **Step 3: Verify section added**

Run:
```bash
cd /Users/twelve/Music/myskills
grep -c "^## \`--maintain\`" harness-workflow/references/maintenance.md
grep -c "memory audit\|suspect detection\|archive sweep\|scorecard rotation" harness-workflow/references/maintenance.md
```

Expected: both ≥ 1.

- [ ] **Step 4: Commit**

Run:
```bash
cd /Users/twelve/Music/myskills
git add harness-workflow/references/maintenance.md
git commit -m "feat(harness): maintenance.md adds --maintain memory audit section"
```

---

## Task 13: Update `harness-workflow/prompts/qa-prompt.md`

**Files:**
- Modify: `harness-workflow/prompts/qa-prompt.md` (essentially replace)

- [ ] **Step 1: Read current content**

Run:
```bash
cd /Users/twelve/Music/myskills
cat harness-workflow/prompts/qa-prompt.md
```

- [ ] **Step 2: Replace with new content**

Use Write tool to overwrite. Full new content:

```markdown
# Stage 6: QA Prompt (via strict-reviewer)

> 本文件是 Stage 6 QA coordinator 的**域上下文模板**。实际审稿由 `strict-reviewer` skill 执行（见 `references/reviewer-integration.md`）。
>
> **不要直接用本文件作为 role prompt** — 它的内容作为 `review_target.claims_to_verify` 或 `diff_summary` 的一部分传给 strict-reviewer。

## 调用方式

Stage 6 coordinator 构造 `review_target`：

```yaml
review_target:
  stage: "qa"
  changed_files:     # from: git diff --name-only <baseSha>..HEAD
    - "<file>"
  diff_summary: |
    # 本轮摘要（一句话）
    ...
  claims_to_verify:  # 每条都来自 PRD 或 plan
    - "新函数 X 覆盖边界情况 Y（empty, max length, concurrent call）"
    - "新 endpoint Z 返回错误码 ABC 按 spec"
    - "集成场景 M→N→O 正确流转"
  memory_cases:      # from .harness-status.json.memoryCheck.matches
    - <case refs>
  prior_verdict: null  # or prior output if retry
```

然后：

```
Skill(skill="strict-reviewer", args=<review_target YAML>)
```

## QA 特定的 claims 选择指引

- **单元测试**：对每个修改的函数，claim = "函数 X 的行为满足 <条件>"（行为描述，不是 "有测试"）
- **集成测试**：对每个跨模块调用链，claim = "模块 A → B → C 的数据流在 <场景> 下正确"
- **E2E**（仅前端有 UI 时）：claim = "用户流 <路径> 在 <浏览器> 下完成 <目标>"

## 覆盖率底线（从 .harness-context.json）

- 新增单元测试覆盖率 ≥ `context.testCoverageThreshold`（默认 60%）
- P0 bug 必须修复（strict-reviewer 会 FAIL）
- P1 bug 进入 knownIssues

## 失败处理

strict-reviewer 返回 FAIL → Stage 6 coordinator：
1. 对每个 `findings[].reproduction`，写 failing test
2. 跑 test，确认 FAIL
3. 修 code
4. 跑 test，确认 PASS
5. 重新 invoke strict-reviewer（同一 `review_target` + `prior_verdict=<上轮 output>`）
6. 最多 3 轮。3 轮仍 FAIL → 升级用户，knownIssues 记录

strict-reviewer 返回 BLOCKED（非 FAIL）→ 停止 Stage 6，上报用户。BLOCKED 意味着输入或 coverage 有问题，不是代码问题。

## 历史上下文（可选）

旧版 qa-prompt.md（v0.x，persona-driven）已归档为 `prompts/qa-prompt-legacy.md`（如有）。新流程下 role prompt 的"人格"表达完全由 strict-reviewer 的 default-FAIL 立场负责。
```

- [ ] **Step 3: Verify no persona cruft left**

Run:
```bash
cd /Users/twelve/Music/myskills
grep -c "你是一位 QA\|你是.*测试工程师" harness-workflow/prompts/qa-prompt.md || echo "CLEAN"
grep -c "strict-reviewer" harness-workflow/prompts/qa-prompt.md
```

Expected: CLEAN (no persona text) + strict-reviewer mentioned.

- [ ] **Step 4: Commit**

Run:
```bash
cd /Users/twelve/Music/myskills
git add harness-workflow/prompts/qa-prompt.md
git commit -m "feat(harness): qa-prompt.md becomes domain-context template for strict-reviewer"
```

---

## Task 14: Update `harness-workflow/prompts/security-prompt.md`

**Files:**
- Modify: `harness-workflow/prompts/security-prompt.md`

- [ ] **Step 1: Replace with new content**

Same pattern as Task 13. Use Write tool. Full new content:

```markdown
# Stage 7: Security Prompt (via strict-reviewer)

> 本文件是 Stage 7 Security coordinator 的**域上下文模板**。实际审稿由 `strict-reviewer` skill 执行。
>
> **不要直接用本文件作为 role prompt** — 内容作为 `review_target.claims_to_verify` 传给 strict-reviewer。

## 调用方式

```yaml
review_target:
  stage: "security"
  changed_files:     # from: git diff --name-only <baseSha>..HEAD
    - "<file>"
  diff_summary: |
    ...
  claims_to_verify:
    # OWASP-style 具体威胁陈述，不要泛泛"安全"
    - "<endpoint> 对未认证用户返回 401，不泄漏是否存在对应资源"
    - "<form> 的用户输入在写入 DB 前经过 parameterization，无 SQL 注入路径"
    - "session token 存储使用 httpOnly + Secure cookie，不可 JS 访问"
  memory_cases: [...]
  prior_verdict: null
```

## Security 特定的 claims 模板

- **注入类**：参数化/预编译/allow-list + 编码
- **认证/授权**：每个新 endpoint 的权限检查，避免 IDOR / BOLA
- **数据泄漏**：错误消息 / response / log 不含敏感字段
- **CSRF / CORS**：state-changing 操作有 token 或 same-site cookie
- **依赖链**：`{context.auditCommand}` 输出无新增 high/critical CVE
- **密钥**：无 hardcoded secret（hook 层也扫但 security claim 里重复强调）

## 合规 hook（如适用）

若 `.harness-context.json.complianceFrameworks` 非空（e.g., `["SOC2", "GDPR"]`），每个框架列表中自动追加对应 claims 模板。（模板在 `references/security-compliance-claims.md`，本 spec 未覆盖该文件，独立后续工作。）

## 失败处理

与 Task 13 一致：FAIL → 写 failing test + 修 + 再 invoke，最多 3 轮；BLOCKED → 上报用户。
```

- [ ] **Step 2: Verify**

Run:
```bash
cd /Users/twelve/Music/myskills
grep -c "strict-reviewer" harness-workflow/prompts/security-prompt.md
grep -c "你是.*安全工程师" harness-workflow/prompts/security-prompt.md || echo "CLEAN"
```

Expected: strict-reviewer ≥ 1; CLEAN.

- [ ] **Step 3: Commit**

Run:
```bash
cd /Users/twelve/Music/myskills
git add harness-workflow/prompts/security-prompt.md
git commit -m "feat(harness): security-prompt.md becomes domain-context template for strict-reviewer"
```

---

# Phase D — team-commander Session Health Check

## Task 15: Update `team-commander/SKILL.md` — Step 6 split

**Files:**
- Modify: `team-commander/SKILL.md`

- [ ] **Step 1: Read current Step 6 block**

Run:
```bash
cd /Users/twelve/Music/myskills
grep -n "^### Step 6\|^## Step 6\|Step 6" team-commander/SKILL.md | head -5
sed -n '/^### Step 6/,/^### Step 7\|^## /p' team-commander/SKILL.md | head -80
```

Record exact current text.

- [ ] **Step 2: Replace Step 6 with split version**

Use Edit tool. Replace the `### Step 6: 上下文健康检查` block (or equivalent) with:

```markdown
### Step 6: Session Health Check

> Split into two concerns (spec §team-commander Step 6 重命名):
> - **6.1 Context pressure** — preserved from previous version
> - **6.2 Persistent memory drift** — new, reads `.harness-memory.yml`

#### 6.1 Context pressure (unchanged from previous version)

<preserve existing logic: warn when session context is getting full, suggest
/compact or hand-off. Do not modify.>

#### 6.2 Persistent memory drift (new)

在每次 round 开始前，检查目标项目的 `docs/memory/.harness-memory.yml` 的漂移信号：

**A. Contract validity**
- `.harness-memory.yml` exists?
- 解析不抛异常?
- `forbidden_paths` 非空?
- `owned_paths` 无 broad unscoped?
- 任一 fail → warn user, do NOT auto-start new round

**B. Suspect state backlog**
- Scan `docs/memory/cases/harness_*.md` for `freshness.state: suspect`
- If > 3 cases suspect for > 14 days → "suspect backlog" warning
- User can:
  - 逐条审核 (convert suspect → active with new `last_verified`, or → archived)
  - 忽略（下 round 再提示）

**C. Audit timestamps drift**
- 检查 `.harness-memory.yml.audits.last_full_audit`
- If null or > 90 days old → suggest `/harness-workflow --maintain`

**D. Scorecard health**
- Read `docs/memory/harness_reviewer_scorecard.yml`
- `false_pass_incidents` count / `totals.pass_count` > 5%? → warn "PASS false-pass rate elevated"
- Latest 10 reviews 内 BLOCKED > 3? → warn "reviewer infra unstable"

Full spec → `harness-workflow/references/memory.md` §5.3 + `references/maintenance.md` §`--maintain` memory audit.
```

- [ ] **Step 3: Verify edits**

Run:
```bash
cd /Users/twelve/Music/myskills
grep -c "Session Health Check\|6\.1 Context pressure\|6\.2 Persistent memory drift" team-commander/SKILL.md
grep -c "\.harness-memory\.yml" team-commander/SKILL.md
```

Expected: first count ≥ 3, second count ≥ 1.

- [ ] **Step 4: Commit**

Run:
```bash
cd /Users/twelve/Music/myskills
git add team-commander/SKILL.md
git commit -m "feat(team-commander): split Step 6 into Session Health Check (6.1 context + 6.2 memory drift)"
```

---

# Phase E — Verification + push

## Task 16: End-to-end grep + cross-reference verification

**Files:**
- Read only (verification)

- [ ] **Step 1: All new/modified files exist**

Run:
```bash
cd /Users/twelve/Music/myskills
# New files
for f in \
  strict-reviewer/SKILL.md \
  harness-workflow/references/memory.md \
  harness-workflow/references/memory-migrations.md \
  harness-workflow/references/reviewer-integration.md \
  harness-workflow/templates/project-memory/.harness-memory.yml.template \
  harness-workflow/templates/project-memory/MEMORY.md.template \
  harness-workflow/templates/project-memory/ERRORS.md.template \
  harness-workflow/templates/project-memory/cases/README.md \
  harness-workflow/templates/project-memory/decisions/README.md \
  harness-workflow/templates/project-memory/constraints/README.md \
  harness-workflow/templates/project-memory/archive/README.md; do
  test -f $f && echo "OK: $f" || echo "MISSING: $f"
done
```

Expected: all OK.

- [ ] **Step 2: No orphan references**

Run:
```bash
cd /Users/twelve/Music/myskills
# Every reference like references/foo.md that's linked from anywhere must exist
grep -rEh "references/[a-z-]+\.md" harness-workflow/ strict-reviewer/ 2>/dev/null | \
  grep -oE "references/[a-z-]+\.md" | sort -u | \
  while read r; do
    full="harness-workflow/$r"
    test -f $full && echo "OK: $full" || echo "BROKEN: $full"
  done
```

Expected: all OK, no BROKEN.

- [ ] **Step 3: Template YAML renders**

Run:
```bash
cd /Users/twelve/Music/myskills
sed -e 's/{{project_name}}/acme/' \
    -e 's/{{project_type}}/nextjs/' \
    -e 's|{{root_fingerprint}}|package.json:name=acme|' \
    harness-workflow/templates/project-memory/.harness-memory.yml.template | \
    python3 -c "
import sys, yaml
d = yaml.safe_load(sys.stdin)
assert d['schema_version'] == '1.0.0'
assert d['forbidden_paths']
assert 'reviewer' in d and 'scorecard_path' in d['reviewer']
print('Template OK')
"
```

Expected: `Template OK`

- [ ] **Step 4: strict-reviewer frontmatter is valid**

Run:
```bash
cd /Users/twelve/Music/myskills
python3 -c "
import yaml
content = open('strict-reviewer/SKILL.md').read()
parts = content.split('---', 2)
fm = yaml.safe_load(parts[1])
assert fm['name'] == 'strict-reviewer'
assert len(fm['description']) >= 100
print('Frontmatter OK')
"
```

Expected: `Frontmatter OK`

- [ ] **Step 5: Claude sees strict-reviewer skill**

Run:
```bash
readlink ~/.claude/skills/strict-reviewer
ls ~/.claude/skills/strict-reviewer/SKILL.md
```

Expected: symlink target + SKILL.md exists.

- [ ] **Step 6: Map every spec scenario to implementation point**

Manually (this is a checklist, not a command):
- [ ] Scenario 1 (`--init`) → Task 2 + Task 10 Phase 3
- [ ] Scenario 2 (`--adopt`) → Task 10 Phase 3 item 4
- [ ] Scenario 3 (Stage 3 ERRORS) → Task 11 Step 3
- [ ] Scenario 4 (three gates BLOCKED) → Task 8 strict-reviewer SKILL.md
- [ ] Scenario 5 (false-pass) → Task 9 reviewer-integration.md §Linking false-pass incidents
- [ ] Scenario 6 (suspect) → Task 11 Step 8 (Stage 8)
- [ ] Scenario 7 (baseSha) → Task 11 Step 2
- [ ] Scenario 8 (post-diff scan) → Task 11 Step 3 Step 4
- [ ] Scenario 9 (user case read-only) → Task 6 §4.4 + strict-reviewer (Task 8) input spec
- [ ] Scenario 10 (broad rejection) → Task 10 Phase 3 item 5
- [ ] Scenario 11 (scorecard rotation) → Task 9 §5 + Task 11 Step 8 item 7

All spec scenarios are addressed. If any remain uncovered, add tasks and re-verify.

- [ ] **Step 7: No plan placeholders**

Run:
```bash
cd /Users/twelve/Music/myskills
grep -rn "TODO\|TBD\|FIXME\|implement later" \
  harness-workflow/references/memory.md \
  harness-workflow/references/memory-migrations.md \
  harness-workflow/references/reviewer-integration.md \
  strict-reviewer/SKILL.md \
  harness-workflow/prompts/qa-prompt.md \
  harness-workflow/prompts/security-prompt.md \
  2>&1 | grep -v "^Binary" || echo "CLEAN"
```

Expected: `CLEAN`

- [ ] **Step 8: Commit verification notes (optional)**

If verification uncovered nothing to commit, skip. Otherwise amend last commit or create `chore: post-implementation verification notes` commit.

---

## Task 17: Update iteration log with implementation completion

**Files:**
- Modify: `docs/superpowers/specs/2026-04-22-iteration-log.md`

- [ ] **Step 1: Append completion section**

Use Edit tool. Append to end of `docs/superpowers/specs/2026-04-22-iteration-log.md`:

```markdown

---

## 实施完成（本 commit）

Plan: `docs/superpowers/plans/2026-04-22-memory-reviewer-implementation.md`

按 17 个 task 顺序实施（Phase A-E），新建 11 文件 + 修改 7 文件。

### 关键验证通过

- ✅ `.harness-memory.yml.template` YAML 渲染无异常
- ✅ strict-reviewer SKILL.md frontmatter 合法，已 symlink 到 `~/.claude/skills/strict-reviewer`
- ✅ 11 个 spec 场景全部映射到实现点
- ✅ 所有 references/*.md 交叉引用无 broken link
- ✅ 无 TBD / TODO / FIXME 残留

### 下一步

- 跟踪 false-pass incidents（运行一个真实 round 后观察 scorecard）
- 如 spec §Open Questions 遗留内容，后续 spec 补充
```

- [ ] **Step 2: Commit**

Run:
```bash
cd /Users/twelve/Music/myskills
git add docs/superpowers/specs/2026-04-22-iteration-log.md
git commit -m "docs: iteration-log marks implementation complete"
```

---

## Task 18: Push to origin

- [ ] **Step 1: Confirm all tasks done**

Run:
```bash
cd /Users/twelve/Music/myskills
git log --oneline origin/main..HEAD | head -25
git status --short
```

Expected:
- Multiple new commits ahead of origin
- `git status` clean

- [ ] **Step 2: Push**

Run:
```bash
cd /Users/twelve/Music/myskills
git push origin main
```

Expected: push succeeds, no conflicts.

- [ ] **Step 3: Verify remote state**

Run:
```bash
cd /Users/twelve/Music/myskills
git fetch origin
test "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)" && echo "SYNCED" || echo "DRIFTED"
```

Expected: `SYNCED`

- [ ] **Step 4: Final summary**

Print implementation summary to the user:

```
✅ Implementation complete.

New skill: strict-reviewer (symlinked to ~/.claude/skills/)
harness-workflow upgraded: memory contract + 4-step Stage 3 enforcement +
strict-reviewer invocation across Stage 4/5/6/7 + team-commander Session
Health Check 6.1/6.2.

Commits pushed to origin/main. Next: run /harness-workflow --init on a real
project to exercise the scaffold, and let the reviewer PASS/FAIL/BLOCKED
flow happen end-to-end.
```

---

## Self-Review (plan author checks)

**1. Spec coverage:** Every spec scenario (1-11) has a task point; see Task 16 Step 6 checklist. Every spec 实施范围 file (18 entries: 11 new + 7 modified) has a dedicated task.

**2. Placeholder scan:** No TBD / TODO in this plan — every step has runnable commands or full file content. Exception: some "preserve existing logic" instructions in Task 15 Step 2 — these refer to concrete existing content the engineer can read in the file, not to be invented.

**3. Type consistency:** `review_target` / `scorecard_delta` / `.harness-status.json.memoryCheck` structures are consistent across Task 8 / Task 9 / Task 11. File paths are absolute-prefixed with `harness-workflow/` / `strict-reviewer/` / `team-commander/` uniformly.

**4. Ambiguity check:** One ambiguous call — "prompts/qa-prompt-legacy.md (if exists)" in Task 13. Resolution: don't create legacy file proactively; only name exists if the engineer renames the old file. The Edit tool in Task 13 overwrites qa-prompt.md with new content; no legacy file is created by this plan.

No issues found that require plan rewrite.

---

## Execution handoff

Plan complete and saved to `docs/superpowers/plans/2026-04-22-memory-reviewer-implementation.md`. Two execution options:

**1. Subagent-Driven (recommended)** — Dispatch fresh subagent per task (Task 1, Task 2, ...), review between tasks, fast iteration. Best for this plan's scale (18 tasks, mostly independent markdown writes).

**2. Inline Execution** — Execute tasks in this session using `superpowers:executing-plans`, batch execution with checkpoints.

Which approach do you want?
