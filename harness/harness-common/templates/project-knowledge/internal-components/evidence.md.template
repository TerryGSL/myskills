---
domain: internal-components
snapshot_id: "{{snapshot_id}}"
---

# Evidence

此文件由 scanner 生成，记录 `internal-components/manifest.md` 中每条 Rule 的代码证据。
**用途**：仅供 audit 使用；Stage -0.5 不加载此文件（只加载 manifest.md）。
**硬约束**：总行数 ≤ 220 行；超出时合并次要 example。

**格式要求**（scanner 填充时必须遵守）：
- 每个 anchor 必须与 manifest.md 中对应 Rule 的 `Evidence:` 字段锚点一致
- 每个 example 必须包含 `**File**: <path>:<line>` 和 `**Interpretation**: <一句话解读>`
- 禁止纯 path glob / 无 file:line 的引用 / 无来源代码块
- 至少 1 个正例（核心模块）；反例可选（边界证据）

<!-- EVIDENCE_BLOCKS_START -->
{{evidence_blocks}}
<!-- EVIDENCE_BLOCKS_END -->

<!-- Evidence block 格式范例（scanner 填充 evidence_blocks 时使用此格式）：

## result-wrapper-usage

Supporting "业务层 service 必须通过 Result<T> 包装返回值":

### Example 1 (positive, central)
- **File**: `src/main/java/com/acme/core/service/OrderService.java:34`
- **Interpretation**: createOrder() 返回 Result<OrderDTO>，调用方通过 result.isSuccess() 判断结果，无异常控制流。

### Example 2 (positive, central)
- **File**: `src/main/java/com/acme/core/service/UserService.java:67`
- **Interpretation**: getUserById() 同样返回 Result<UserDTO>，包含 empty 分支处理，证明此为通用约定。

### Counterexample (optional, boundary / deprecated)
- **File**: `src/main/java/com/acme/legacy/OldPaymentService.java:23`
- **Interpretation**: 遗留实现抛出 PaymentException，已标记 @Deprecated，与当前 Result<T> 约定冲突，属历史遗留。

---

注意：anchor 名称（如 result-wrapper-usage）必须与 manifest.md 对应 Rule 的
**Evidence**: evidence.md#<anchor> 中的 anchor 完全一致（大小写、连字符均须匹配）。
-->
