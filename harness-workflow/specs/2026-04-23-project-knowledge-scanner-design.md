# harness-workflow: 项目级 knowledge 自动扫描与注入机制

**日期**: 2026-04-23
**状态**: Design approved, ready for writing-plans
**决策者**: TerryGSL
**协作**: Claude (brainstorm orchestrator), Codex (architecture pressure-test, 2 rounds)

---

## Context

### 问题

harness-workflow 当前的接入流程（`team-init`）只做技术栈粗粒度探测（language/framework/projectType + 3 个 build/test/lint 命令），进入存量项目（尤其公司级老 Java repo）后：

- 不了解已有代码风格和约定
- 不知道内部组件 / util / SDK 可复用
- 不知道该用什么框架处理异常 / i18n / 上下游
- 写出"初学者味道"代码，融入不了已有风格
- **质量反而不如 codex 直接结合仓库写**

用户已经确认方向：
1. 全自动精细扫描（2-3 轮），尽量不打扰用户
2. 多 subagent 并行 + codex 协同扫描
3. 扫描后集中成**批量 TODO**，用户最后一次性回答 ≤8 条
4. Knowledge 沉淀为**分散 markdown 文件**（按主题），不塞进 skill frontmatter
5. 由**项目级 skill/协议**做索引 + 调用触发，公共 skill 保持复用
6. 切换 repo 自动切换 knowledge 源

### 目标

在 harness-workflow 中增加**项目级 knowledge 扫描与注入机制**，让 harness 在存量项目里写代码时：

1. 进场前主动扫描一次，沉淀 local knowledge
2. 每轮开发前自动注入相关 manifest 到 Stage 2/3 subagent
3. strict-reviewer 强制检查实现是否遵循 manifest Rule
4. knowledge 随代码演化可检测 drift / 可 rescan / 可回灌

### Non-goals

- 不替代 `team-init` 的技术栈探测（那是 prerequisite）
- 不替代 `docs/memory/` 的积累型知识（那是互补）
- 不在公司级 knowledge base（Confluence 等）集成（out of scope）
- 不做"代码规范自动修复"（scanner 只记录，不改代码）

---

## 架构（Option D：Adaptive Domain-Sharded Evidence Manifests）

### 三个新产物

**产物 1：Scanner 基础设施（在 harness-workflow skill 内新增）**

```
harness-workflow/
├── references/
│   ├── project-scanner.md              ← 新：scanner 完整规范
│   └── knowledge-retrieval.md          ← 新：Stage -0.5 + retrieval 协议
├── prompts/
│   └── scanner-prompts.md              ← 新：5 domain scanner 的 prompt 模板
├── templates/
│   └── project-knowledge/               ← 新：目标项目脚手架模板
│       ├── INDEX.md.template
│       ├── TODO.md.template
│       ├── style-and-structure/
│       │   ├── manifest.md.template
│       │   ├── evidence.md.template
│       │   └── gaps.md.template
│       ├── internal-components/ (同上三文件)
│       ├── exception-and-error-contracts/ (同上)
│       ├── integrations-and-sdk-usage/ (同上)
│       └── i18n-and-text-boundaries/ (同上)
```

**产物 2：harness-workflow 主 skill 集成**

```
harness-workflow/
├── skill.md                             ← 改：新增 Phase 5 (--scan-project 触发)
├── references/
│   ├── workflow.md                      ← 改：新增 Stage -0.5 Project Context Retrieval
│   ├── reviewer-integration.md          ← 改：review_target 新 3 字段
│   ├── maintenance.md                   ← 改：--maintain 新增 6 条 knowledge audit
│   └── memory.md                        ← 小改：说明 knowledge vs memory 分工
```

**产物 3：strict-reviewer 新增第 4 硬门**

```
strict-reviewer/SKILL.md                 ← 改：Required Steps 加 Step 5 Knowledge Compliance Check
                                            Verdict 决定规则加 knowledge violation 判断
```

### 在目标项目生成的产物（repo-local，git 追踪）

```
<target-repo>/
├── docs/harness/knowledge/
│   ├── INDEX.md                         ← 根索引，shared harness 启动时必读
│   ├── TODO.md                          ← scanner 留给用户批量回答（≤8 条）
│   ├── style-and-structure/             ← 总是激活
│   │   ├── manifest.md      ≤ 140 行     ← 紧凑规则，运行时检索
│   │   ├── evidence.md      ≤ 220 行     ← file:line 证据
│   │   └── gaps.md          (conditional)
│   ├── internal-components/              ← 总是激活
│   │   └── manifest/evidence/gaps
│   ├── exception-and-error-contracts/
│   ├── integrations-and-sdk-usage/       ← 条件激活（有 SDK 才扫）
│   └── i18n-and-text-boundaries/         ← 条件激活（有 resource bundles 才扫）
└── CLAUDE.md                             ← 自动追加 <!-- harness-knowledge:start --> 触发契约块
```

### 职责边界

| 产物 | 唯一负责 |
|------|---------|
| Scanner 基础设施 | 5 阶段扫描 pipeline、subagent 分派、codex 协同、TODO 聚合 |
| Stage -0.5 | 每轮读 INDEX → 选 relevant_knowledge_files → 注入下游 Stage |
| strict-reviewer 第 4 硬门 | 验证实现是否违反 knowledge Rule |
| `docs/harness/knowledge/` 目标项目 | 知识载体（跟 repo 走、跨设备同步）|
| `CLAUDE.md` 触发契约块 | 告知 Claude 该 repo 有 knowledge 要先读 |

### 保持不变

- `team-init` 只做技术栈探测 + 目录骨架（scanner 不合并进 team-init）
- `docs/memory/` 的 cases/decisions/constraints 机制不变（knowledge 互补，不替代）
- 其他 team-* skill（pd/architect/senior/junior/qa/security）形态不变，只是 task prompt 多一段 knowledge 注入

---

## Knowledge 存储格式

### INDEX.md（shared harness 运行时入口）

完整 schema：

```markdown
# Project Knowledge INDEX

Generated by harness-workflow scanner — do not edit manually except user-notes blocks.

## Scan Metadata
- schema_version: "1.0.0"
- snapshot_id: "scan-YYYY-MM-DDTHH:MMZ"
- scanner_version: "1.0.0"
- last_full_scan: "ISO timestamp"
- scan_duration_sec: <int>
- active_domains: [<list>]
- total_files_sampled: <int>
- total_todos_surfaced: <int>
- confidence_summary: {high: N, medium: N, low_dropped: N}
- status: active | stale | drifted    # 不含 disabled（disable 是用户决策，只在 CLAUDE.md 表达，不由 scanner 写 INDEX）

## Domain Map (retrieval-ready)

| domain | applies_to (glob) | confidence | last_verified | status |
|--------|------------------|------------|---------------|--------|

## Retrieval Routing Rules

Rules Stage -0.5 uses to select relevant_knowledge_files for a task:
- Path globs (primary)
- Keyword triggers (secondary)
- Always-load domains (unconditional)

## Open TODOs

See TODO.md (count: <N>)

## User Overrides

（由 Phase 5 `--apply-knowledge-answers` 生成，Stage -0.5 读取此处作为 Advisory Context 输入）

| gap_id | domain | summary | source_gap_anchor |
|--------|--------|---------|-------------------|
| <domain>/gap-<N> | <domain> | <一句话总结用户约定> | <domain>/gaps.md#gap-<N> |

## Expired Free-Form Rules

（由 `--maintain` 步骤 7 检测并移入此处；Stage -0.5 作为 Advisory Context 注入，不再绑定 FAIL）

| rule_id | domain | last_verified | expiry_after_days | requirement_text |
|---------|--------|---------------|-------------------|-----------------|
| <domain>/rule-<N> | <domain> | YYYY-MM-DD | <int> | <一句话描述> |
```

### `<domain>/manifest.md`（≤ 140 行，运行时检索）

每条 Rule 固定格式：

```markdown
---
domain: <name>
snapshot_id: "scan-..."
applies_to:
  paths: ["glob/**"]
last_verified: YYYY-MM-DD
status: active | partial | drifted | superseded_by:<file>
---

# <Domain> — Rules

## Rule: <一句话规则标题>

**Rule ID**: <domain>/rule-<N>     # 稳定 ID，用于 supersedes 引用 + cleanup 精确匹配
**规则**: <具体描述，≤ 3 段话>
**适用**: <path glob>
**Evidence**: evidence.md#<anchor>
**Confidence**: high
**Status**: active | expired | drifted | superseded   # 四态详解见下
**Supersedes Gap ID**: <optional, 例: internal-components/gap-2>     # 若此 rule 由 user override 升级而来
**Supersedes Rule ID**: <optional, 例: internal-components/rule-3>   # 若此 rule 取代了一个老 rule
**violation_test**: <enum>        # 见 Activation 段枚举；仅 active + structured 的参与 binding 检查
```

**硬格式约束**：
- 每条 Rule 必须有 `Rule ID`（稳定 ID，不可变）
- 每条 Rule 必须有 `Evidence:` 指向 evidence.md anchor
- 每条 Rule 必须 `Confidence: high`（medium / low 不出现在 manifest）
- 每条 Rule 必须有 `Status`（默认 `active`；由 `--maintain` 或 cleanup 流程可改为 `expired` / `drifted` / `superseded`）
- `Supersedes Gap ID` / `Supersedes Rule ID` 是 optional inline metadata（不是 YAML frontmatter），用于 Phase 5 / rescan 精确清理旧条目
- Rule 描述 ≤ 3 段
- manifest 超 140 行 → scanner 必须挤掉 low-impact 规则

**Rule Status 四态运行时影响**：

| Status | 意义 | Stage -0.5 处理 | reviewer 行为 |
|---|---|---|---|
| `active` | 正常有效的 rule | 加入 `knowledge_requirements`（binding）| 违反 → FAIL |
| `expired` | free_form_review rule 过期，降级为主观 advisory | **不**加入 `knowledge_requirements`；镜像进 `advisory_knowledge`（source: expired_rule）| 不 FAIL，仅作提示 |
| `drifted` | drift detection 发现该 rule 被 >30% 代码违反（即代码演化走向了新约定，老 rule 过时）| **不**加入 `knowledge_requirements`；**也不**进 advisory（避免误导）；记 knownIssue 提示用户 `--partial-rescan` | 不触发 |
| `superseded` | 该 rule 已被另一条 rule 取代（通常是 expired rule 被新 evidence 重救活形成新 active rule）| **不**加入任何上下文（既不 binding 也不 advisory）；保留在 manifest 作历史记录，后续 `--maintain` 可选归档 | 不触发 |

**drifted 与 expired 的区别**：
- `expired`：free_form_review rule 的时间到期，rule 本身没过时，只是无法机器验证
- `drifted`：rule 已**过时**（代码演化走了另一条路），应该通过 rescan 更新或删除，而不是保留为 advisory

**per-rule Status vs per-manifest frontmatter status 的分工**：
- per-rule `Status: active|expired|drifted` → 单条规则级别
- manifest frontmatter `status: active|partial|drifted|superseded_by` → 整个 manifest 文件级别（scanner 失败等整体状态）

两者各管各的层级，不冲突。

### `<domain>/evidence.md`（≤ 220 行，audit 用）

```markdown
---
domain: <name>
snapshot_id: "scan-..."
---

# Evidence

## <anchor-matching-manifest-rule>

Supporting "<rule title>":

### Example 1 (positive, central)
- **File**: `<path>:<line>`
- **Interpretation**: <一句话解读>

### Example 2 (positive, central)
- **File**: `<path>:<line>`
- **Interpretation**: ...

### Counterexample (optional, boundary / deprecated)
- **File**: `<path>:<line>`
- **Interpretation**: ...
```

**硬格式约束**：
- 每个 anchor 对应 manifest 的一条 Rule
- 每个 example 必须：`File:` 带路径+行号 + `Interpretation:` 一句话
- 禁止纯 path glob / 无 file:line 的引用 / 无来源代码块
- 至少 1 个正例（核心模块）；可选 1 个反例（边界证据）

### `<domain>/gaps.md`（仅当 scanner 有未解时生成）

```markdown
---
domain: <name>
snapshot_id: "scan-..."
---

# Unresolved Gaps

Scanner 扫到但无法达到 high confidence 的条目。详见 TODO.md。

- **Gap 1**: <问题描述>
  - 分裂证据: <file:line (A 用法)> vs <file:line (B 用法)>
  - 用户回答位置: TODO.md#todo-<N>
```

### TODO.md（用户批量回答入口）

```markdown
# Batch Q&A — Project Knowledge Gaps

Scanner found <N> gaps it couldn't resolve from code alone.
Answer inline; run `/harness-workflow --apply-knowledge-answers` when done.

## TODO-<N>: <一句话问题>

**Context**: <scanner 发现的证据冲突 / 空缺>
**Question**: <具体决策问题>
**Your answer**: _____

---

## TODO-<N+1>: ...
```

---

## Scanner Pipeline（5 阶段，总上限 28 min）

### Phase 1: Scout（8 min，主 coordinator 自跑）

- `ls` / `rg` 快速遍历（不做全文 AST 分析）
- 识别技术栈（复用 `.harness-context.json`）
- 识别模块边界：top-level package 树、主要目录
- 探测 domain 激活信号：
  - resource bundles / `messages_*.properties` / i18n helpers → 激活 `i18n-and-text-boundaries`
  - SDK clients / adapter packages / HTTP client wrappers → 激活 `integrations-and-sdk-usage`
  - 其他 domain 按发现程度决定
- `style-and-structure` + `internal-components` **总是激活**
- 输出：`.harness-status.json.scoutReport`（临时字段）

### Phase 2: Parallel Domain Scan（12 min，N 个 subagent 并行）

N = 激活 domain 数，max 5。每个 subagent：

1. 拿 scout 给出的 domain boundary
2. 用 rg / symbol index 无限制搜索
3. 选 ≤ 24 个 representative 文件做 full read
   - representative = 核心模块（import count 高）+ 最近改动 + 边界模块
4. 提取 high-confidence convention：
   - **≥ 2 正例** 或 **1 正 + 1 反**
   - 每条带 `file:line` + interpretation
5. Medium confidence（1 个强例子在核心模块）→ TODO buffer（高影响才留）
6. Low confidence（弱信号）→ **直接丢**
7. 产出 `manifest-draft.md` + `evidence-draft.md` 到对应 domain 目录

Subagent prompt 硬门（见 `prompts/scanner-prompts.md`）：
- 不扫 domain 边界外文件
- 不编造 convention
- 不照搬 legacy 坏风格（标记 deprecated pattern 放反例）

### Phase 3: Codex Contradiction Pass（5 min）

Codex 以只读模式跨模型审查：
- 找 domain 间矛盾（两个 manifest 是否冲突）
- 找遗漏（scout 激活的 domain 是否漏关键 convention）
- 找编造（manifest Rule 能否在 evidence 对上号）
- 产出 `codex-contradictions.md`（临时）

### Phase 4: TODO Aggregation（3 min）

主 coordinator 合并：
- domain scanner 的 medium TODO（若高影响）
- codex 发现的不确定
- 去重、排序（核心模块 > 边界）
- **硬上限 8 条** — 超出则丢 low-impact
- 写入 `docs/harness/knowledge/TODO.md`

### Phase 5: User Batch Answer → Evidence-Backed Finalize

1. ⏸ **HALT**。提示用户看 TODO.md
2. 用户在每条 `Your answer:` 后填答案
3. 用户跑 `/harness-workflow --apply-knowledge-answers`
4. Coordinator 读 TODO 答案，**分支处理**（evidence-first 契约不可破）：
   - 每条用户答案 → 写入对应 `<domain>/gaps.md` 的 `resolved_by_user` 块（保留用户原话作"override 声明"）
   - 立即跑 **micro-rescan**：按该答案指引的 convention，用 rg/symbol 搜 2 个支持该 convention 的 file:line 示例
   - **若找到 ≥ 2 正例** 或 **1 正 + 1 反** → 该规则升级为 high confidence，进 manifest.md；**新 rule 的 inline metadata fields（在 rule block 内）必须声明 `**Supersedes Gap ID**: <此次用户答案的 gap_id>`**（不是 YAML frontmatter，是 rule block 内的 `**Field**:` 行）；evidence.md 补充 anchor + examples；**按 ID 精确清理旧的 user override**：
     - 把 `<domain>/gaps.md` 里 gap_id 精确匹配的 `resolved_by_user` 块标记为 `superseded_by_rule: <new_rule_id>`（保留历史但标注已被取代）
     - 从 INDEX.md `## User Overrides` 表按 gap_id 精确移除对应行
     - 禁止基于 text similarity 删除 — 防止误伤
   - **若找不到足够 evidence** → 规则保留在 gaps.md 作为 `explicit user override`，**不进 manifest**；Stage -0.5 注入时标注"用户声明约定（未核实）"并降低权重
5. 生成最终 INDEX.md（含 snapshot_id + routing rules + user-override 声明清单）
6. 删除所有 `*-draft.md` 临时文件
7. 追加 CLAUDE.md 触发契约块（幂等）
8. `git commit`（用户授权前 **不 auto push**）

**为什么不直接把用户答案升成 high confidence**：evidence-first 契约要求 manifest 每条 rule 有 file:line 支撑。用户答案 ≠ 代码证据。若代码里不存在符合答案的实例，这说明约定只存在于用户脑中 —— 这种信息写入 manifest 会让 reviewer 错误 FAIL 代码。用 gaps.md + user-override 的形式保留信息但不污染 manifest。

### 失败 / 降级

| 场景 | 动作 |
|------|------|
| Scout 失败 | BLOCKED，scanner 不跑 |
| 某 domain scanner 超时（> 3 min） | 该 domain 标 `status: partial`，confidence 降级 |
| codex 不可用 | 跳过 Phase 3，INDEX metadata 标注 `cross_model_pass: skipped` |
| 用户不回答 TODO.md | knowledge 可用（只含 high confidence 部分），gaps.md 保留；后续 `--maintain` 再提醒 |
| 总耗时超 28 min | 当前 Phase 完成后 BLOCKED，用户决定延长还是接受 partial 结果 |

### Scan Budget 硬上限

| 项 | 上限 | 依据 |
|---|---|---|
| 总 wall-clock | 28 min | 平衡完整性与响应性 |
| 每 scanner full file read | 24 个 | 足够采样，不溺水 |
| 总 TODO 数（呈现用户）| 8 | 用户明示"尽量少" |
| manifest.md 大小 | 140 行 | 超出 retrieval 质量崩 |
| evidence.md 大小 | 220 行 | evidence 是 audit，不是主检索 |
| 最大激活 domain | 5 | 超出说明分类有问题 |

---

## Activation / Retrieval 协议

### Stage -0.5: Project Context Retrieval

**何时**：每个 Round 开始时，在 Stage 0/1/2 之前。**S 级也不跳过**。

**流程**：

**Step 0: Disable check**（先于一切）

读目标项目 `CLAUDE.md` 的 `<!-- harness-knowledge:start --> ... <!-- harness-knowledge:end -->` 块：
- 若块内含 `harness-knowledge: disabled`（任意位置）→ 内存中设 `effective_index_status = disabled`，**跳过 Stage -0.5 其余步骤**（与 INDEX.status 无关）
- 若块不存在 或 无 `disabled` 标记 → 继续

**为什么用 CLAUDE.md 而非 INDEX.status**：disable 是**用户决策**，CLAUDE.md 是用户主笔区；INDEX.md 是 scanner 产物，用户不该被迫编辑。让 disable 开关在用户手边最自然。

---

1. 检查 `docs/harness/knowledge/INDEX.md` 存在性
   - 不存在 → 跳过 Stage -0.5（项目未接入 knowledge，走普通 harness 模式）
   - 存在 → 继续（INDEX.status 的 `active | stale | drifted` **都不跳过** Stage -0.5；三者区别只在注入时的 warning level 和哪些 manifest/rule 进入 `knowledge_requirements`。详见生命周期状态机）；**disable 只通过 CLAUDE.md 表达**，已在 Step 0 处理

2. 读 INDEX.md 的 Retrieval Routing Rules 段

3. 匹配任务 → 选 `relevant_knowledge_files`：
   - 若本轮 changed_files 已知 → path glob 匹配
   - 若只有需求文本 → keyword 匹配 + always-load
   - always-load：`style-and-structure` + `internal-components`

4. 加载每个命中 manifest.md（**只读 manifest，不读 evidence**）

4a. **加载相关 advisory knowledge**（新）：
   - 从 INDEX.md 的 `## User Overrides` 表读命中 domain 的条目 → 每条转 `{source: "user_override", id: gap_id, domain, text: <gaps.md 里对应 resolved_by_user 块的完整文本>, weight: "advisory"}`
   - 从 INDEX.md 的 `## Expired Free-Form Rules` 表读命中 domain 的条目 → 每条转 `{source: "expired_rule", id: rule_id, domain, text: requirement_text, weight: "advisory"}`
   - 合并为单一 `advisory_knowledge` 数组（统一 schema，两种来源共存）
   - **这些条目绝不进 `knowledge_requirements`**（reviewer 不以 advisory 作 FAIL 依据）
   - 注入到下面步骤 5

5. 写入 `.harness-status.json`（结构化，**不用 free-form string**）：

**路径严格限制**：`relevant_knowledge_files` 只允许 `docs/harness/knowledge/**/manifest.md`。Memory（`docs/memory/`）走 Stage 3 的 ERRORS 单独通道（见 memory.md），**不混入** knowledge 注入，防止 caller 用错 corpus 还过 schema。

```json
{
  "knowledgeCheck": {
    "snapshot_id": "scan-...",
    "relevant_knowledge_files": ["docs/harness/knowledge/internal-components/manifest.md", ...],
    "advisory_knowledge": [
      {
        "source": "user_override",
        "id": "internal-components/gap-2",
        "domain": "internal-components",
        "text": "新 service 使用 constructor injection（用户声明，未在代码里找到 high-confidence 证据）",
        "weight": "advisory"
      },
      {
        "source": "expired_rule",
        "id": "exception-and-error-contracts/rule-5",
        "domain": "exception-and-error-contracts",
        "text": "（原 free_form_review rule：避免在 Controller 层直接 catch RuntimeException）— last_verified 已过期",
        "weight": "advisory"
      }
    ],
    "knowledge_requirements": [
      {
        "rule_id": "internal-components/rule-1",
        "manifest_file": "docs/harness/knowledge/internal-components/manifest.md",
        "applies_to": ["src/main/java/com/acme/core/service/**"],
        "requirement_text": "业务层 service 必须返回 Result<T>，禁止抛 BusinessException",
        "violation_test": "must_use_wrapper",
        "wrapper_type": "Result"
      },
      {
        "rule_id": "exception-and-error-contracts/rule-3",
        "manifest_file": "docs/harness/knowledge/exception-and-error-contracts/manifest.md",
        "applies_to": ["src/main/java/**"],
        "requirement_text": "不得直接 throw RuntimeException 裸类型",
        "violation_test": "must_not_throw_raw_exception",
        "exception_types": ["RuntimeException", "Exception"]
      }
    ]
  }
}
```

**`violation_test` 取值枚举**（可扩展）：

| 值 | 含义 | 配套字段 |
|---|---|---|
| `must_use_wrapper` | 必须返回某 wrapper 类型 | `wrapper_type: <class>` |
| `must_call_component` | 必须通过某组件调用 | `component: <package.Class>` |
| `must_not_throw_raw_exception` | 禁止抛某些裸异常 | `exception_types: [<class>]` |
| `must_use_package` | 必须导入/调用某 package | `package: <prefix>` |
| `must_not_use_pattern` | 禁止某代码模式（regex 或 AST 签名）| `pattern: <regex>` |
| `must_annotate_with` | 必须带某注解 | `annotation: <class>` |
| `free_form_review` | 无法机器检查，交 LLM 判断 | `requirement_text` + `manual_review_reason` + `expiry_after_days` |

`free_form_review` 是保底，但 scanner/manifest 生成时应尽量匹配到前 6 种结构化类型。

**`free_form_review` 额外约束**（防止"永久主观 rule 不可驳"）：
- 必须带 `manual_review_reason`（为何无法结构化的解释，一句话）
- 必须带 `expiry_after_days`（默认 90 天）
- `--maintain` 步骤 7（下方定义）检查 `free_form_review` rule 的 `last_verified`：
  - 若 `now - last_verified > expiry_after_days` → 做两件事：
    - 在 manifest.md 中标注 `status: expired`（不删除 Rule，但 reviewer 不再以此作 FAIL 依据）
    - 在 INDEX.md 的 `## Expired Free-Form Rules` 表追加一行（让 Stage -0.5 注入时把它合并进 `advisory_knowledge`）
  - 除非被 `/harness-workflow --partial-rescan <domain>` 重新采证据或用户手动 bump `last_verified` → 从 INDEX `Expired Free-Form Rules` 表移除 + manifest 恢复 `status: active`
- 这样任何"能 FAIL 但无法机器校验"的 rule 都有明确退出机制

6. 注入到 Stage 2 / Stage 3 subagent 的 task prompt 前置

### Task prompt 注入规范

Stage 2/3 每个 subagent prompt 自动 prepend：

```
# Project Knowledge Context（由 Stage -0.5 预查，本任务相关）

## Binding Rules（来自 manifest，违反 → reviewer FAIL）

以下 manifest 来自 docs/harness/knowledge/：

### <domain>/manifest.md
<manifest 全文>

## Advisory Context（来自 user overrides 和过期 free_form_review rule，非强制）

以下是用户声明的约定或已过期的主观规则，**尽量遵循但 reviewer 不会以此为 FAIL 依据**：

- [<gap_id>] <override_text>
- [<expired-rule-id>] <requirement_text>（last_verified 已过期）

---

**硬契约**：
1. Binding Rules 必须严格遵循，违反 → reviewer FAIL
2. Advisory Context 作为风格参考，尽量遵循但非强制
3. 若 Binding Rule 与需求冲突 → 停下上报，不要自行决定违反
4. 输出中 echo 一行 "Knowledge check:" 证明你已消化
5. 对每条 Binding Rule，给出遵循证据（file:line 或 test:case）
```

### Stage 4 入口门

```
0. Disabled bypass：读 .harness-status.json.knowledgeCheck.effective_index_status
   若 = "disabled"（Stage -0.5 Step 0 已设置）→ 跳过 knowledge gate，直接进 strict-reviewer（不做 knowledge-grounding 检查）
1. 读 .harness-status.json.knowledgeCheck
2. 若 snapshot_id 为空且 INDEX 存在 AND effective_index_status != "disabled" → BLOCKED
   （coordinator 漏跑 Stage -0.5 但 knowledge 启用中，是系统错误；无 recovery，必须升级用户）
3. 若实际变更文件涉及 manifest 的 applies_to，但 relevant_knowledge_files 没包含对应 manifest
   → 进入 **Stage 4 Late Recovery**（见下）
4. 通过 → 进 strict-reviewer
```

同步约定：Stage -0.5 Step 0 必须把 `effective_index_status` 写入 `.harness-status.json.knowledgeCheck`：
```json
"knowledgeCheck": {
  "effective_index_status": "disabled" | "active" | "stale" | "drifted",
  "snapshot_id": "scan-..." | null,
  ...
}
```

### Stage 4 Late Recovery（keyword-only retrieval 漏了 manifest 的自动补救）

当 Stage 4 发现 retrieval 漏 manifest（Stage -0.5 是基于需求文本 keyword 做的匹配，后来实际 diff 涉及了更多 domain），**不立即升级用户**，执行以下 recovery（最多 1 次）：

1. coordinator 用**实际 git diff**（`git diff --name-only <baseSha>..HEAD`）重跑 Stage -0.5 的路径匹配部分（不重跑 scout）
2. 生成更新的 `relevant_knowledge_files` + `knowledge_requirements`（可能新增 N 个 manifest）
3. **对每个新加入的 domain，再跑 Stage -0.5 步骤 4a**（读 INDEX 的 `## User Overrides` + `## Expired Free-Form Rules` 表 + 对应 `gaps.md`）→ append 条目到 `advisory_knowledge`
4. 更新 `.harness-status.json.knowledgeCheck`（保留原 snapshot_id；补充 requirements + advisory_knowledge）
5. **若 recovery 新增的 manifest 含 `knowledge_requirements`**（注意：只含 `Status: active` 的 rule；expired rule 已被 Stage -0.5 排除并转为 advisory）：
   - 不直接进 reviewer
   - 先 dispatch 一个 remediation task 到 Stage 3：让 subagent 读新 manifest（含 binding rules + Advisory Context），检查当前实现是否违反 binding rules，若违反则修
   - remediation 完成 → 重进 Stage 4 入口门（走完整检查）
6. **若 recovery 后仍 BLOCK**（例如 recovery 本身执行失败 / 新 manifest 文件缺失）→ **升级用户**
7. recovery 只跑 **1 次**，避免循环。第二次 late BLOCK 直接升级用户

Recovery 是"retrieval 漏召回"的自动修复。本质上承认 keyword-based pre-retrieval 有局限，真正的 ground truth 是 diff，所以最后用 diff 兜底。

### strict-reviewer 第 4 硬门

`review_target` 新增字段：

```yaml
relevant_knowledge_files: [...]
knowledge_snapshot_id: "scan-..."
knowledge_requirements: [...]
```

Required Steps 加第 5 步：

```
Step 5: Knowledge Compliance Check
  对 knowledge_requirements 每条：
  - 读对应 manifest.md 确认 Rule 存在
  - 读 diff 变更代码，验证是否遵循
  - 违反 → finding severity=high, message="violated knowledge rule: <rule>"
```

Verdict 决定规则扩展：

| 条件 | verdict |
|---|---|
| 任一 knowledge_requirement 被违反 | FAIL |
| INDEX 存在但 `relevant_knowledge_files` 为空，**且原因是 coordinator 未填写**（未跑 Stage -0.5） | BLOCKED |
| INDEX 存在但 `relevant_knowledge_files` 为空，**且原因是所有 candidate manifest 都 drifted / superseded 被系统过滤** | 不 BLOCK；记 knownIssue；warn "所有相关 manifest 过时/被取代，建议跑 `/harness-workflow --partial-rescan <domain>` 或 `--rescan`"，允许本轮继续（无 knowledge binding，但不死锁）|
| 其他 | 按原规则 |

### CLAUDE.md 触发契约（scanner 完成后自动追加）

```markdown
<!-- harness-knowledge:start -->
## Harness Knowledge Activation

This project has `docs/harness/knowledge/INDEX.md`. Before any harness-workflow round:

1. Harness coordinator MUST run Stage -0.5 (Project Context Retrieval)
2. Stage 2/3 subagent prompts MUST include knowledge context
3. Stage 4 MUST verify knowledge_requirements compliance

To disable: set `harness-knowledge: disabled` below this block.
<!-- harness-knowledge:end -->
```

幂等：已有该块则跳过。

---

## Maintenance / Update

### knowledge vs memory 分工（最终确定）

| 维度 | knowledge（本 spec）| memory（已实施）|
|---|---|---|
| 时机 | 接入时一次性扫描生成 | 开发过程逐轮积累 |
| 内容 | 代码 idiom / 内部组件 / 约定 / SDK 用法 | bug cases / 架构决策 / 遗留约束 |
| 来源 | AI 读现有代码 + 用户批量回答 | 每轮 Stage 2/6/7/8 实时沉淀 |
| 位置 | `docs/harness/knowledge/<domain>/` | `docs/memory/{cases,decisions,constraints}/` |
| 更新频率 | 低（`--rescan`）| 高（每轮可能加）|
| 老化机制 | `snapshot_id` + `last_full_scan` | `freshness.last_used` + suspect 状态机 |

两者互补：Stage -0.5 先读 knowledge（静态 inventory），Stage 3 再读 memory 的 ERRORS（动态教训）。

### `--maintain` 新增 knowledge audit（6 条）

在已有 memory audit 后追加：

7. **Knowledge snapshot freshness**
   - `last_full_scan > 90 天` → warn 建议 `--rescan`
   - `> 180 天` → mark `INDEX.status: stale`，Stage -0.5 注入时加 warning

8. **Knowledge drift detection**（occurrence-based，非文件级采样）
   - 对每个 manifest 的每条 Rule：
     - 用该 Rule 的 `violation_test` + `applies_to` glob，在对应代码里采**最多 10 个 matched occurrences**（例如：所有 service 方法、所有抛异常点、所有 SDK 调用点）
     - 对每个 occurrence 评分：`compliant` / `noncompliant` / `not_applicable`
     - 计算违反率 = `noncompliant / (compliant + noncompliant)`
   - Drift 触发条件（**同时满足**）：
     - 违反率 > **0.3**
     - `sample_size = compliant + noncompliant >= 5`（少于 5 个样本不下结论，避免小样本噪声）
   - 命中 → **设该 Rule 的 per-rule `Status: drifted`**（不是 manifest 级别），drifted rule 清单写入 `gaps.md`
   - manifest 级别 frontmatter `status` 只有在**多数 Rule 都 drifted**（>50%）才设 `drifted`
   - `violation_test: free_form_review` 的 Rule 无法机器采样 → 降级为人工抽查提示（TODO.md 追加）
   - drifted 的单条 Rule **既不**进 `knowledge_requirements`（不 binding），**也不**进 `advisory_knowledge`（避免误导），只记 knownIssue
   - 聚合到 TODO.md：用户决定"更新 manifest（跑 partial-rescan）"还是"修代码遵循 manifest"

9. **Evidence file:line 有效性**
   - 验证每个 evidence 里的 file:line 仍存在（git blame 判行号漂移）
   - 失效 → gaps.md 记录，`--rescan` 时重新抓证据

10. **TODO.md 未回答项**
    - 超过 30 天未回答 → warn

11. **INDEX.md 自身 drift**
    - `active_domains` 声明但 manifest 不存在 → BLOCKED
    - manifest 存在但 INDEX 未列 → warn（孤儿文件）

12. **knowledge ↔ memory 反向链接一致性**
    - `memory/cases/*` 里 `applies_to_knowledge` 指向的 manifest rule 必须存在
    - `knowledge/<domain>/evidence.md` 的反例若链接 `memory/cases/*` 必须存在

### Rescan 模式

```
/harness-workflow --rescan                # 全部重扫（5-phase full pipeline，28 min）
/harness-workflow --partial-rescan <domain>  # 只重扫一个 domain（≤ 10 min）
```

Partial rescan 快速路径：
- 只激活指定 domain 的 scanner
- Scout **必须局部重跑 boundary 探测**（不能纯复用上次 scout）：
  - 重新识别该 domain 的 applies_to glob 是否变化（包/目录重命名、边界移动）
  - 若发现 boundary 变化 → 同步更新 INDEX.md 的 Domain Map；旧 boundary 下的 manifest 做一致性检查（若仍有 evidence 留下，否则归档 manifest 到 `superseded_by`）
- 若 scout 发现**新增 domain**（之前未激活的，现在有激活信号）→ partial-rescan 提示用户改跑 `--rescan` full 模式
- Codex Contradiction Pass 仍跑（防新 manifest 与旧冲突）
- TODO 聚合上限 ≤ 3 条
- **User Override cleanup**（stable ID 精确匹配，禁止 fuzzy text similarity）：
  - 每条新升级的 high-confidence rule，其 rule block 的 inline metadata fields 必须声明 `**Supersedes Gap ID**: <gap-N>` 或 `**Supersedes Rule ID**: <old-rule-N>`（inline 行，不是 YAML frontmatter）— 这是**显式来源关联**
  - Cleanup 逻辑只按**显式 ID 匹配**进行：
    - 若 rule 声明 `supersedes_gap_id: X` → 标记 `<domain>/gaps.md#X` 为 `superseded_by_rule: <new-rule-id>` + 从 INDEX `## User Overrides` 移除 gap_id=X 的行
    - 若 rule 声明 `supersedes_rule_id: Y` → 从 INDEX `## Expired Free-Form Rules` 移除 rule_id=Y 的行 + 在对应 manifest 里保留旧 rule 但标 `status: superseded`
  - **禁止 text similarity 自动删除**：text similarity 只能 propose candidate 写入 TODO，让用户确认后再 bump `supersedes_*` ID 手动触发精确清理
  - Rationale：大型 repo 里相似表述跨 domain 常见，自动 fuzzy 删除会误伤（codex Round 6 Finding 2）

### 与 strict-reviewer 的反向反馈闭环

reviewer 发现"代码违反 manifest Rule" → 产 finding，**不自动更新 manifest**。代码修好后：
1. manifest 不变（代码错，knowledge 对）
2. scorecard 记录 review 事件

反之，若 manifest 过时：
1. 用户看到 FAIL 后手动判断 manifest 已过时
2. 跑 `/harness-workflow --partial-rescan <domain>`
3. scanner 重采证据，若新约定 high confidence → manifest 更新 + snapshot_id bump
4. 下次 reviewer 读新 manifest，不再 FAIL

**硬约束**：manifest 更新必须走 scanner，**不接受** reviewer/主 agent 直接改 manifest。

### 生命周期状态机

```
INDEX.status (scanner 写入，不含 disabled):
  active    → Stage -0.5 正常注入所有 eligible manifest
  stale     → last_full_scan > 180 天，retrieval 照常跑，注入时加 "knowledge is stale" warning
  drifted   → 至少一个 manifest 或大量 rule 已漂移，retrieval 照常跑，注入时加 "some manifests drifted, run --partial-rescan" warning；**drifted 的单个 rule 按 Rule Status 处理（不进 binding 也不进 advisory）**；drifted 的 manifest 级 status 仅作警告不导致 skip

effective_index_status (Stage -0.5 运行时计算，读 CLAUDE.md):
  active | stale | drifted  → 同 INDEX.status
  disabled  → CLAUDE.md 内含 `harness-knowledge: disabled`，本轮跳过所有 Stage -0.5 和 Stage 4 knowledge gate

manifest.status (单 domain, 整 manifest 级别):
  active    → 正常
  partial   → scanner 超时或 scout 不全，信心降级
  drifted   → drift detection 发现多数 rule (>50%) 违反
  superseded_by <newer>  → rescan 后旧版归档

evidence.status:
  active    → 所有 file:line 有效
  stale     → >50% file:line 漂移
```

---

## 实施范围

### 新建文件

| 路径 | 用途 |
|------|------|
| `harness-workflow/references/project-scanner.md` | Scanner 5 阶段 pipeline 完整规范 |
| `harness-workflow/references/knowledge-retrieval.md` | Stage -0.5 + Retrieval 协议 |
| `harness-workflow/prompts/scanner-prompts.md` | 5 domain scanner prompt 模板 |
| `harness-workflow/templates/project-knowledge/INDEX.md.template` | INDEX 模板 |
| `harness-workflow/templates/project-knowledge/TODO.md.template` | TODO 模板 |
| `harness-workflow/templates/project-knowledge/<5-domains>/{manifest,evidence,gaps}.md.template` | 15 个 domain 模板文件（5 domain × 3 类型）|

### 修改文件

| 路径 | 改动 |
|------|------|
| `harness-workflow/skill.md` | 新增 Phase 5（`--scan-project` / `--rescan` / `--partial-rescan` / `--apply-knowledge-answers` 触发）|
| `harness-workflow/references/workflow.md` | 新增 Stage -0.5 定义 + Stage 4 入口门 knowledge 检查 |
| `harness-workflow/references/reviewer-integration.md` | `review_target` 新 3 字段；routing PASS/FAIL/BLOCKED 规则扩展 |
| `harness-workflow/references/maintenance.md` | `--maintain` 新增 6 条 knowledge audit（编号 7-12）|
| `harness-workflow/references/memory.md` | 小改：显式声明 knowledge vs memory 分工 |
| `strict-reviewer/SKILL.md` | Required Steps 加 Step 5 Knowledge Compliance Check；Verdict 规则扩展 |

### 不动

- `team-init/SKILL.md`
- `team-pd/SKILL.md`
- `team-architect/SKILL.md`
- `team-senior-dev/SKILL.md`
- `team-junior-dev/SKILL.md`
- `team-qa/SKILL.md` / `team-security/SKILL.md`
- `team-commander/SKILL.md`
- 现有 `.harness-memory.yml` / `docs/memory/` 机制

---

## 验证方式

### 端到端场景

1. **新项目 `--scan-project` 完整 run**：
   - 中型 Java 项目 (500 文件) 跑 `/harness-workflow --scan-project`
   - 验证：28 min 内完成；生成 5 domain 目录（≤ 5 个激活）；INDEX.md 有 metadata；TODO.md ≤ 8 条

2. **Scanner 硬门：编造检测**：
   - 人为在 scanner 产出里注入一条无 evidence 的 convention
   - 验证：Codex Contradiction Pass 捕获；该 rule 被降级到 TODO 或丢弃

3. **Stage -0.5 注入**：
   - 已有 INDEX 的项目，用户提"加一个新 endpoint"
   - 验证：Stage -0.5 选到 `internal-components/manifest.md`；Stage 2 task prompt 含 knowledge 块；Stage 3 subagent output 有 "Knowledge check:" 行

4. **Knowledge violation → reviewer FAIL**：
   - manifest 有 "use Result<T>"，Stage 3 subagent 故意抛 BusinessException
   - 验证：strict-reviewer Step 5 发现 violation，verdict=FAIL，finding 指向具体 file:line

5. **BLOCKED 链路**：
   - 删掉 .harness-status.json.knowledgeCheck 后进 Stage 4
   - 验证：Stage 4 入口 BLOCKED，上报用户

6a. **User TODO batch answer — 有 evidence 支持的答案（应升级进 manifest）**：
   - TODO.md 含一条问题，用户回答："Service 应使用 constructor injection"
   - 代码里确实有 ≥2 个 service 使用 constructor injection（micro-rescan 能采到 evidence）
   - 跑 `/harness-workflow --apply-knowledge-answers`
   - 验证：答案升级为 high confidence，进 `<domain>/manifest.md` + `evidence.md` 补 anchor + file:line；snapshot_id bump；TODO.md 相应条目清空

6b. **User TODO batch answer — 无 evidence 支持的答案（应留 gaps.md，不进 manifest）**：
   - TODO.md 含一条问题，用户回答："新 service 应统一使用 Spring constructor injection"
   - 代码里实际没有 ≥2 个 service 遵循此约定（micro-rescan 找不到足够证据）
   - 跑 `/harness-workflow --apply-knowledge-answers`
   - 验证：答案 **不进 manifest**；写入 `<domain>/gaps.md#gap-N` 作 `resolved_by_user` 块；INDEX.md 的 `## User Overrides` 表新增一行；TODO.md 相应条目清空；下次 Stage -0.5 注入时作 Advisory Context（非 FAIL 依据）

7a. **Drift detection — 单条 rule 漂移（manifest 整体不变）**：
   - manifest 有 5 条 rules，其中 Rule-3 说 "use A"，但代码里对应 rule 的 occurrences 中 40% 用 B
   - 跑 `/harness-workflow --maintain`
   - 验证：**Rule-3 的 per-rule `Status: drifted`**；manifest frontmatter 级 `status` 仍 `active`；TODO.md 追加"Rule-3 drifted — 是否更新"；Stage -0.5 注入时 Rule-3 不进 knowledge_requirements 也不进 advisory

7b. **Drift detection — 多数 rule 漂移（manifest 整体 drifted）**：
   - manifest 有 5 条 rules，其中 3 条都 drift（>50%）
   - 跑 `/harness-workflow --maintain`
   - 验证：manifest frontmatter 级 `status: drifted`；每条 drifted 的 rule 也标 `Status: drifted`；整个 manifest 在 INDEX 的 Domain Map 里更新 status；Stage -0.5 注入时该 manifest 整体不参与

8. **Partial rescan**：
   - 跑 `/harness-workflow --partial-rescan internal-components`
   - 验证：只重扫该 domain；其他 domain manifest 不变；10 min 内完成

9. **CLAUDE.md 触发契约幂等**：
   - 跑两次 `--scan-project`
   - 验证：CLAUDE.md 只有一个 `<!-- harness-knowledge:start -->` 块

10. **Disabled 模式**：
    - 在 CLAUDE.md 写 `harness-knowledge: disabled`
    - 跑一轮开发
    - 验证：Stage -0.5 跳过，harness 以普通模式运行

### 回归检查

```bash
cd <target-repo>

# 1. 所有 manifest 可 parse
for f in docs/harness/knowledge/*/manifest.md; do
  head -20 $f | grep -qE "^---$" && echo "✓ $f" || echo "✗ $f"
done

# 2. manifest rule 都有 evidence anchor
grep -rn "^\*\*Evidence\*\*:" docs/harness/knowledge/*/manifest.md | while read line; do
  anchor=$(echo "$line" | grep -oE "evidence.md#[a-z0-9-]+")
  # 验证 anchor 存在于对应 evidence.md
  # ...
done

# 3. 所有 evidence file:line 可访问
grep -rnE "\\*\\*File\\*\\*: \`[^:]+:[0-9]+\`" docs/harness/knowledge/*/evidence.md
# 取每个 file:line，验证文件 exists

# 4. INDEX.md 与 manifest 一致性
# active_domains 每一个都要在目录中存在对应 manifest.md
```

---

## Resolved Decisions

1. **5 domains (不是 6)**：build-test-runtime 折叠进 style 或 integrations，除非 scout 发现异常到需独立
2. **manifest / evidence / gaps 三文件分离**：manifest 运行时检索、evidence 仅 audit、gaps 条件生成
3. **只 high confidence 进 manifest**：medium → TODO（高影响才留）；low → 丢
4. **evidence 必须 file:line + interpretation**：禁止纯 path glob / 无来源代码块
5. **loading 机制 = CLAUDE.md + git-tracked markdown**：不依赖 Claude Code 的 `.claude/commands/` 或 `.claude/skills/` 的 repo-local 机制（未验证）
6. **Scanner coordination = adaptive over fixed taxonomy**：fixed 5 domain 作为存储 shape，scout 决定哪些激活
7. **Scan budget 硬上限**：28 min / 24 files-per-scanner / 8 TODOs / 140 manifest / 220 evidence / 5 domains
8. **Stage -0.5 universal**：S 级也不跳过
9. **manifest 更新必须走 scanner**：reviewer / 主 agent 不直接改 manifest
10. **knowledge / memory 分工**：前者静态 inventory（一次扫 + rescan），后者动态积累（逐轮）

---

## Open Questions（留给 writing-plans 阶段决定）

*（按 Round 4 教训：spec 里不留 Open Questions。下面 3 条其实是实施细节，spec 阶段不重要）*

1. Scanner subagent 的 cost budget — 每个 subagent 跑多少 tokens 合理？（实施时测后定）
2. Codex 不可用时的 fallback — 是否改用 Claude opus 自己做第 3 phase？（实施时看 Claude-mode 质量）
3. `--apply-knowledge-answers` 的 UX —— 用户跑完命令后 harness 如何提示下一步？（实施细节）

---

## 附：设计协作记录

- Round 1 Codex：3 skeletons + 3 新约束（budget / evidence / activation）
- Round 2 Codex：收敛到 Option D + 6 个具体问题答案（hybrid、enforcement、evidence bar、loading、coordination、budget）
- User 确认：3 约束全收，骨架接受 D，进入分节设计

Codex session ID: `.context/codex-session-id` (可 `/codex` 追问)

---

## 下一步

按 superpowers:brainstorming skill 终态规定，下一步是 invoke `superpowers:writing-plans` skill 把本 spec 变成可执行 implementation plan。

Implementation scope 预估：
- 🆕 新建 ~ 21 文件（scanner 规范 + prompts + 15 个模板文件 + 2 个 reference）
- ✏️ 修改 ~ 6 文件（skill.md / workflow.md / reviewer-integration.md / maintenance.md / memory.md / strict-reviewer SKILL.md）
- 复杂度：比上次 memory/reviewer 实施略大（scanner 是新工作流，需要 pipeline 编排）
